# Overnight Automated Code Review TODO

Started: 2026-07-06 23:38 Europe/Istanbul
Target branch: `dev`
Review base HEAD: `1ecfbeb3688dde36fac9917990189fed6b1f3406`
Cadence: once every 20 minutes until 2026-07-07 09:00 Europe/Istanbul

The chunk manifest and machine-local run state live at:

```text
C:\Users\messy\.codex\automation-state\ll-tools-overnight-code-review
```

## Operating Rules

- Review the next pending chunk from the manifest on each heartbeat.
- Treat this as a code-review pass: prioritize correctness, security, performance, large-wordset behavior, i18n readiness, capability/nonce checks, missing tests, and stale docs.
- Append follow-up items under `Findings TODO` with chunk id, severity, evidence, suggested fix, and suggested verification.
- Safe minor fixes may be made immediately, especially documentation, source-contract, typo, or narrow test-maintenance updates.
- Do not do broad refactors, live-site writes, release/version bumps, UI redesigns, or product behavior changes as part of the heartbeat unless a chunk reveals a small clear bug fix with focused verification.
- Commit completed repo changes after each heartbeat pass, following `AGENTS.md`.
- Stop instead of mutating if the branch or current allowed HEAD in the machine-local state no longer matches the checkout.

## Coverage Plan

The run is sized for 28 review chunks, which fits the remaining 20-minute slots before the 2026-07-07 09:00 Europe/Istanbul cutoff. Chunks cover tracked first-party source, docs, scripts, tests, styles, templates, locale sources, and first-party offline-app builder code. Vendor, generated artifacts, binary media, test reports, built release output, and the embedded third-party Whisper/GGML source tree are excluded from normal overnight coverage.

## Findings TODO

- `chunk-02-data-model-roles` [P2] Content lesson edit screens preload option rows for every wordset.
  Evidence: `includes/post-types/content-lesson-post-type.php:1073-1098` builds `rowsByWordset`, `prereqRowsByWordset`, and `prereqLessonRowsByWordset` for every wordset whenever a content lesson edit screen enqueues its admin JS. Those helpers include unbounded per-wordset lesson/category reads, for example `ll_tools_get_content_lesson_prereq_lesson_option_rows()` at `includes/post-types/content-lesson-post-type.php:715-729` and the selected-row fill-in query at `includes/post-types/content-lesson-post-type.php:959-967`.
  Likely impact: a site with many wordsets, categories, or content lessons pays the cost on every content lesson edit page, even when the editor is only working in one wordset. The localized JS payload can also become very large.
  Suggested fix: localize only the current lesson wordset plus an empty/default state, then lazy-load category/prereq lesson options for a newly selected wordset through a nonce- and `view_ll_tools`-guarded AJAX/REST endpoint with bounded result shape.
  Suggested verification: add/extend a content-lesson admin test that seeds multiple wordsets with many categories/lessons and asserts the initial localized payload only contains the active wordset; add a focused browser/admin check that switching wordsets fetches options once and keeps the edit page responsive.
- `chunk-02-data-model-roles` [P2] Dictionary admin filters scan or render all dictionary entries.
  Evidence: the Words list-table filter builds a `<select>` by fetching every `ll_dictionary_entry` ID at `includes/post-types/dictionary-entry-post-type.php:1662-1688`. The dictionary entry list-table filtering path calls `ll_tools_dictionary_entry_get_admin_list_ids()` at `includes/post-types/dictionary-entry-post-type.php:1878-1888`, then loops every entry in `includes/post-types/dictionary-entry-post-type.php:2258-2303`; the linked/unlinked branch calls `ll_tools_count_dictionary_entry_words()` for each entry, which runs a `WP_Query` at `includes/post-types/dictionary-entry-post-type.php:842-863`.
  Likely impact: large dictionary imports can turn normal admin list loads into full-table scans plus many per-entry queries, and the Words filter dropdown can produce thousands of `<option>` elements.
  Suggested fix: replace the Words dropdown with the existing bounded dictionary search/autocomplete pattern, and make dictionary-entry filters SQL/meta-query driven or backed by a compact materialized index instead of iterating all entries in PHP.
  Suggested verification: seed several thousand dictionary entries with mixed source/wordset/linked states, then assert filtered admin queries do not call `get_posts()` for every entry and do not run per-entry linked-count queries.
- `chunk-03-wordset-category-taxonomies` [P2] Line-Up category settings synchronously load and render every word in the category.
  Evidence: `ll_tools_get_category_lineup_allowed_word_ids()` fetches every `words` post ID in a category with `posts_per_page => -1` at `includes/taxonomies/word-category-taxonomy.php:1667-1683`. `ll_tools_get_category_lineup_word_items()` then hydrates every matching word post for the edit UI at `includes/taxonomies/word-category-taxonomy.php:1832-1847`, and the taxonomy edit form renders one sortable row per item at `includes/taxonomies/word-category-taxonomy.php:2215-2254`. The vocab lesson category settings path also includes the same full item list in its settings payload at `includes/pages/vocab-lesson-pages.php:3498-3511`, and both save paths validate submitted order by reloading the full allowed-word list at `includes/taxonomies/word-category-taxonomy.php:2283` and `includes/pages/vocab-lesson-pages.php:4037-4051`.
  Likely impact: a category with thousands of words can make normal category edit or wordset-manager settings pages slow or unusable, and saving the settings repeats the full scan even when only a short order changed.
  Suggested fix: replace the inline all-words sorter with a paged/searchable Line-Up manager that loads candidate words on demand, validates submitted IDs in bounded chunks, and stores order changes through a nonce/capability-guarded endpoint with progress or clear batch limits.
  Suggested verification: seed a large category and assert the initial category/settings render does not include all word rows; add a focused save test proving submitted Line-Up IDs are validated without an unbounded category-wide query.
- `chunk-03-wordset-category-taxonomies` [P2] Wordset grammar option saves can run full-wordset metadata rewrites synchronously.
  Evidence: the normal wordset term save handler `ll_save_wordset_language()` handles grammar option POST data after nonce/capability checks at `includes/taxonomies/wordset-taxonomy.php:3476-3528`. Changing gender/plurality/verb-tense/verb-mood options calls sync helpers at `includes/taxonomies/wordset-taxonomy.php:3840-3843`, `includes/taxonomies/wordset-taxonomy.php:3938-3939`, `includes/taxonomies/wordset-taxonomy.php:3976-3977`, and `includes/taxonomies/wordset-taxonomy.php:4014-4015`. Those helpers start with `ll_tools_wordset_get_word_ids_with_meta_in_wordset()`, which queries all matching words in the wordset with `posts_per_page => -1` at `includes/taxonomies/wordset-taxonomy.php:4449-4465`, then loop each word to update/delete post meta and collect category cache bumps, for example `includes/taxonomies/wordset-taxonomy.php:4499-4543`.
  Likely impact: changing grammar option labels on a large wordset can turn an ordinary settings save into thousands of post-meta reads/writes and term lookups, risking timeouts or partial updates without durable progress.
  Suggested fix: make grammar-option remapping an explicit bounded maintenance job with a dry-run/count step, cursor-backed batches, status/readback, and a small synchronous path only below a safe threshold.
  Suggested verification: add a regression that seeds many words with grammar meta, changes an option list, and asserts the settings handler queues or processes bounded batches instead of scanning every matching word in one request.
- `chunk-03-wordset-category-taxonomies` [P2] Wordset isolation health checks rebuild full-site scans from admin notices.
  Evidence: the isolation health report transient TTL is only 15 minutes at `includes/wordset-isolation.php:16-17`, and `ll_tools_render_wordset_isolation_health_notice()` calls `ll_tools_get_wordset_isolation_health_report()` from `admin_notices` for maintenance-capable users at `includes/wordset-isolation.php:2399-2414`. A cache miss builds all collectors at `includes/wordset-isolation.php:2333-2350`; those collectors query all `words` (`includes/wordset-isolation.php:1971-1978`), all `word_images` (`includes/wordset-isolation.php:2080-2087`), all vocab lessons (`includes/wordset-isolation.php:2184-2191`), and all wordsets (`includes/wordset-isolation.php:2244-2248`). The related auto-repair hook can also run on `admin_init` every 5 minutes and, after detecting one anomaly, calls a full vocab-lesson repair scan at `includes/wordset-isolation.php:764-800` and `includes/wordset-isolation.php:745-752`.
  Likely impact: the first admin page load after a transient miss can unexpectedly scan and inspect the whole site's words, word images, vocab lessons, and wordsets. On production-sized sites this can slow unrelated admin workflows, especially after cache invalidation.
  Suggested fix: move isolation health refresh and auto-repair behind an explicit maintenance action or async job that records durable status; make admin notices read only the last cached status and offer a "check now" action rather than rebuilding the report inline.
  Suggested verification: add a focused admin-notice regression that expires the health transient on a seeded large fixture and asserts ordinary admin page rendering does not call the full collectors; cover the explicit refresh path separately.
- `chunk-07-wordset-pages-3` [P2] Teacher classes front-end preloads unbounded class and user lists.
  Evidence: `ll_tools_wordset_page_render_teacher_classes_view()` calls `ll_tools_teacher_class_get_available_wordsets()` before the login/capability branch at `includes/pages/wordset-pages.php:10642-10644`, then loads all matching classes with `ll_tools_teacher_classes_for_user()` at `includes/pages/wordset-pages.php:10646-10648`, selected-class student progress rows at `includes/pages/wordset-pages.php:10671-10676`, and admin-only assignable teacher/student dropdowns at `includes/pages/wordset-pages.php:10692-10698`. The helper implementations fetch every wordset via `get_terms()` at `includes/teacher-classes.php:102-108`, every class with `posts_per_page => -1` at `includes/teacher-classes.php:431-445`, every potential teacher with `get_users( fields => all_with_meta )` at `includes/teacher-classes.php:463-468`, every potential student the same way at `includes/teacher-classes.php:619-633`, and all selected-class students plus stats at `includes/teacher-classes.php:678-693`. The sidebar also loops every class and calls `ll_tools_teacher_class_get_student_ids()` per row at `includes/pages/wordset-pages.php:10804-10814`.
  Likely impact: a site with many learner accounts, teachers, or classes can make the front-end Classes page slow and memory-heavy, especially for administrators who receive full teacher/student dropdowns. Even guest requests to the classes route pay the all-wordset query before showing the login prompt.
  Suggested fix: defer wordset/class/user lists until after the permission branch, paginate the class and student progress views, replace teacher/student `<select>` controls with nonce-guarded search/autocomplete endpoints, and use aggregate class student counts instead of per-class meta reads during sidebar render.
  Suggested verification: add a teacher-class front-end resource test that seeds many wordsets, classes, teachers, and learners; assert guest rendering does not query all wordsets, admin rendering does not hydrate all users with meta, and selected-class progress returns a bounded page.
- `chunk-07-wordset-pages-3` [P2] Wordset category settings render every category and load all lesson IDs.
  Evidence: `ll_tools_wordset_page_get_managed_category_rows()` starts by fetching every owned category term through `ll_tools_wordset_page_get_owned_category_terms()` at `includes/pages/wordset-pages.php:13236-13237`; that helper calls `get_terms()` without a page size at `includes/pages/wordset-pages.php:13145-13160`. The lesson-count helper loads every vocab lesson ID for the wordset with `posts_per_page => -1` at `includes/pages/wordset-pages.php:13201-13216` and then reads category meta for each lesson at `includes/pages/wordset-pages.php:13217-13227`. The renderer outputs one full edit form per category at `includes/pages/wordset-pages.php:13661-13752`. The delete path also loads all linked lesson IDs with `posts_per_page => -1` at `includes/pages/wordset-pages.php:13387-13407` and deletes them synchronously at `includes/pages/wordset-pages.php:13417-13425`.
  Likely impact: a large wordset with many categories or lessons can turn the front-end Categories settings tool into a very large HTML response and a full lesson scan. Deleting a category with many lessons can run a long synchronous destructive request without durable progress or partial-failure recovery.
  Suggested fix: replace the all-category settings list with a paged/searchable category manager, compute lesson counts with an aggregate query or materialized count, and move destructive category deletion into a dry-run/status/batched action that reports progress and readback.
  Suggested verification: seed hundreds of categories and thousands of vocab lessons, then assert the initial Categories settings render uses a bounded page and aggregate counts; add a deletion regression that proves large linked-lesson cleanup is chunked or explicitly blocked with a batch workflow.
- `chunk-08-wordset-pages-4` [P2] Recorder queue overview scans every category for every assigned recorder.
  Evidence: `ll_tools_wordset_page_get_recorder_queue_rows()` loops assigned recorders at `includes/pages/wordset-pages.php:15300-15325` and, in the non-focused overview, calls `ll_tools_wordset_page_build_recorder_queue_summary_groups()` at `includes/pages/wordset-pages.php:15366-15368`. That helper loops every summary category at `includes/pages/wordset-pages.php:15101-15196` and calls `ll_tools_wordset_page_get_recorder_queue_category_candidate_word_page()` for each category at `includes/pages/wordset-pages.php:15126-15134`. The candidate helper repeatedly fetches word chunks until it finds enough valid queue items or exhausts the category at `includes/pages/wordset-pages.php:14967-15006`, then does the same for image candidates at `includes/pages/wordset-pages.php:15008-15040`, with query calls at `includes/pages/wordset-pages.php:14820-14830` and `includes/pages/wordset-pages.php:14904`.
  Likely impact: the recorder queue overview is visually compact, but a wordset with many categories and mostly completed recordings can still run many per-category/per-recorder scans just to render overview cards. The focused category view is paged, but the overview discovery step can be the expensive part.
  Suggested fix: make the overview count/cache driven: show assigned recorders and cached category queue counts, load only the first bounded set of categories, and refresh/scan missing-recording summaries through an explicit async or batched action. Keep the focused category page as the detailed paged view.
  Suggested verification: seed multiple recorders, many categories, and mostly complete recording coverage; assert the overview performs a capped number of candidate queries independent of category count, while a focused category request still paginates and excludes completed/hidden items.
- `chunk-16-flashcard-quiz-runtime-a` [P2] Quiz-page grid/dropdown cold renders rebuild the full quiz catalog.
  Evidence: `ll_get_all_quiz_pages_data()` loads every published generated quiz page ID with `posts_per_page => -1` at `includes/shortcodes/quiz-pages-shortcodes.php:311-336`. Before looping those pages, it calls `ll_flashcards_build_categories('', ...)` to build metadata for every playable category at `includes/shortcodes/quiz-pages-shortcodes.php:354-374`. For each item without a wordset filter, it can call `ll_get_default_wordset_id_for_category()`; that helper queries all wordsets at `includes/shortcodes/quiz-pages-shortcodes.php:628-635` and loops them with `ll_can_category_generate_quiz()` at `includes/shortcodes/quiz-pages-shortcodes.php:640-647`. Gender support can also rebuild processed category metadata per discovered wordset at `includes/shortcodes/quiz-pages-shortcodes.php:470-490`.
  Likely impact: the day-long cache helps repeat views, but the first anonymous render after an epoch/plugin/locale change can perform all-page, all-category, and potentially category-by-wordset eligibility work in one request for `[quiz_pages_grid]` or `[quiz_pages_dropdown]`. Large sites can see slow public page loads or cache stampedes outside the existing per-category AJAX build lock.
  Suggested fix: back the quiz-page catalog with a compact materialized index keyed by wordset/category cache epochs, or rebuild it in bounded batches with a public stale-while-refresh fallback; avoid per-page default-wordset discovery during render by storing/resolving playable wordset metadata ahead of time.
  Suggested verification: extend `QuizPagesShortcodeWordsetQueryTest` or add a dedicated resource test with many generated quiz pages, categories, and wordsets; assert cold shortcode rendering does not issue an unbounded quiz-page query plus per-category/per-wordset eligibility loops, and that stale cached catalog data remains usable while refresh work is in progress.
- `chunk-16-flashcard-quiz-runtime-a` [P2] Quiz-page sync and cleanup run full catalog maintenance synchronously.
  Evidence: `ll_tools_sync_quiz_pages()` first calls `ll_tools_cleanup_invalid_quiz_pages()` at `includes/pages/quiz-pages.php:1065-1075`; cleanup loads all generated quiz-page records with `posts_per_page => -1` at `includes/pages/quiz-pages.php:1021-1030` and checks each page through `ll_can_category_generate_quiz()` at `includes/pages/quiz-pages.php:1031-1054`. The sync then loads every `word-category` term at `includes/pages/quiz-pages.php:1079-1088` and calls `ll_can_category_generate_quiz()` for each at `includes/pages/quiz-pages.php:1089-1096`. This full sync is scheduled daily from `admin_init` at `includes/pages/quiz-pages.php:1123-1135`, and the manual admin cleanup/source-mtime paths can run similar all-category/all-page loops synchronously at `includes/pages/quiz-pages.php:1447-1453` and `includes/pages/quiz-pages.php:1498-1558`.
  Likely impact: normal admin traffic or WP-Cron can inherit a full quiz-page maintenance pass with no cursor, progress state, or bounded batch size. On production-sized category sets this can time out, delay unrelated admin requests, or partially sync pages without a durable readback.
  Suggested fix: convert quiz-page maintenance into a cursor-backed job with small page/category batches, a lock, status/readback, stale result reuse, and explicit admin controls for "queue/check now" instead of doing full cleanup/sync inline.
  Suggested verification: add an integration test that seeds many quiz pages/categories and asserts scheduled/manual maintenance processes a bounded batch and records remaining work; keep separate tests for forced single-category sync after content/category edits.
- `chunk-17-flashcard-quiz-runtime-b` [P2] Word Option Rules admin renders and saves full-category pair maps synchronously.
  Evidence: `ll_tools_word_option_rules_get_word_posts()` fetches every published `words` post for the selected wordset/category with `posts_per_page => -1` at `includes/admin/word-option-rules-admin.php:336-364`. The admin renderer then loops all words for labels/media at `includes/admin/word-option-rules-admin.php:1423-1434`, collects audio for the complete ID list at `includes/admin/word-option-rules-admin.php:1432-1434`, builds automatic title/translation/recording-text pair maps with nested pair loops at `includes/admin/word-option-rules-admin.php:626-748`, computes similar/image pairs for every word at `includes/admin/word-option-rules-admin.php:1472-1473`, renders one checkbox row per word at `includes/admin/word-option-rules-admin.php:1636-1719`, and renders full word dropdowns plus every blocked pair at `includes/admin/word-option-rules-admin.php:1743-1913`. The save path repeats the full word lookup and pair-map work before updating the option at `includes/admin/word-option-rules-admin.php:2031-2033` and `includes/admin/word-option-rules-admin.php:2167-2188`.
  Likely impact: a large category can make a normal option-rule edit page memory-heavy or unusable, and autosave/manual save can repeat the same O(category words + generated pairs) work inside one admin-ajax or admin-post request. Dense duplicate labels/transcripts or image-similarity matches can produce very large pair tables and payloads.
  Suggested fix: split this into a paged/searchable word option editor with bounded word rows, cached/materialized automatic conflict summaries, and explicit batch/rebuild status for expensive text/image pair discovery. Save only the changed group/pair deltas for the visible or searched rows, and keep full-category recomputation behind a maintenance action with progress/readback.
  Suggested verification: add a resource regression that seeds a category with many words and duplicate labels/transcripts, then asserts initial render and autosave do not hydrate every word, collect audio for every word, or build all pair combinations synchronously; keep existing small-category behavior tests for manual groups, recording-type pair scopes, and image-similarity overrides.
- `chunk-17-flashcard-quiz-runtime-b` [P2] Listening mode queues every selected category after the first startup word.
  Evidence: `initialize()` deliberately avoids bulk category queueing at `js/flashcard-widget/modes/listening.js:1100-1104`, and `tests/e2e/specs/listening-sequence-weighting.spec.js:8-65` only asserts that initial deferral. Once the first target word finishes resource loading, `handlePostSelection()` schedules `queueAllSelectedCategories(loader)` with zero delay at `js/flashcard-widget/modes/listening.js:1818-1824`; the helper loops every selected category and calls `ensureCategoryLoad()` at `js/flashcard-widget/modes/listening.js:877-882`. The fallback waiting paths also call `queueAllSelectedCategories()` for non-startup rounds at `js/flashcard-widget/modes/listening.js:1566-1576`, `js/flashcard-widget/modes/listening.js:1600-1603`, and `js/flashcard-widget/modes/listening.js:2019-2022`. The shared loader serializes category AJAX by default at `js/flashcard-widget/loader.js:435-483`, but the queue length is still unbounded by the current launch or next few listening words.
  Likely impact: a listening launch over a large wordset or many selected categories can enqueue a long tail of category AJAX/cache-miss work immediately after playback starts, even if the user only listens to the first few words or closes the popup. This can warm many category payloads and media preloads outside the bounded current-word path and can compete with the active session on slower devices.
  Suggested fix: make Listening follow the same bounded prefetch-window model used for media: queue only startup plus the next small category/window needed to keep playback continuous, cancel outstanding category work on close/mode switch, and rely on estimated totals rather than full category arrival for progress. Keep an explicit opt-in full-preload mode only for small launches or cached/offline datasets.
  Suggested verification: extend the listening Playwright/source-contract coverage with dozens of selected categories and assert startup plus first-word playback enqueues only a bounded number of category loads, then advances and fetches the next window as needed; add a close/mode-switch check that pending category queues are not allowed to continue warming the old session.
- `chunk-18-dictionary-i18n-cache` [P2] Snapshot imports still create backup snapshots and override cleanup with full-dictionary synchronous scans.
  Evidence: `ll_tools_dictionary_get_exportable_entry_ids()` uses `get_posts()` with `posts_per_page => -1` for all exportable dictionary entries at `includes/lib/dictionary-snapshot.php:174-188`, and `ll_tools_dictionary_build_snapshot()` loops every returned ID into one in-memory snapshot at `includes/lib/dictionary-snapshot.php:227-245` before encoding/writing it at `includes/lib/dictionary-snapshot.php:249-275`. Snapshot imports call `ll_tools_dictionary_import_attach_backup_snapshot()` while creating the job at `includes/admin/dictionary-import-admin.php:716-733` and `includes/admin/dictionary-import-admin.php:794-850`, so the start-job AJAX handler at `includes/admin/dictionary-import-admin.php:1591-1654` can perform the full backup before the batched importer begins. Override cleanup also loops all exportable entries in `ll_tools_dictionary_delete_entries_missing_keys()` at `includes/lib/dictionary-snapshot.php:663-685`, including completion paths at `includes/admin/dictionary-import-admin.php:1077-1100` and `includes/admin/dictionary-import-admin.php:1150`, despite the admin copy promising short import batches at `includes/admin/dictionary-import-admin.php:1887`.
  Likely impact: a large dictionary snapshot import can time out or exhaust memory before the first progress batch is created, and override-mode cleanup can turn the final progress request into an unbounded destructive scan. Skipping backups avoids some work, but the default safe path still has no cursor or durable progress for the backup/export/delete phases.
  Suggested fix: make backup snapshot creation and override cleanup explicit cursor-backed job phases that write snapshot chunks/manifests incrementally, process missing-entry deletes in bounded batches, and expose progress/readback before import chunks run. Keep direct full export as an intentional admin action or move it behind the same job infrastructure when datasets exceed a small threshold.
  Suggested verification: seed thousands of dictionary entries, start a snapshot import, and assert the first AJAX request only creates job/chunk/cursor state instead of calling `get_posts(posts_per_page=-1)` or encoding all entries; add an override-mode fixture proving missing-entry cleanup resumes in bounded delete chunks.
- `chunk-21-export-import-a` [P2] Export batch start still builds the full export payload synchronously.
  Evidence: the JavaScript path posts `ll_tools_start_export_bundle` before any progress loop at `js/export-import-admin.js:849-904`, and that AJAX handler calls `ll_tools_export_prepare_batch_job()` at `includes/admin/export-import.php:4367-4378`. The prepare function immediately calls `ll_tools_build_export_payload()` at `includes/admin/export-import.php:2168-2174`, then JSON-encodes and writes the complete `data.json` payload before returning the first batch response at `includes/admin/export-import.php:2204-2214`. The non-JS admin-post fallback runs the same payload build and writes the entire zip in one request at `includes/admin/export-import.php:4401-4452`.
  Likely impact: media file writes are chunked, but a large full-category or all-category export can still spend a long first request hydrating all export data and attachment metadata before the progress screen moves. The fallback path can also time out while writing the zip synchronously, leaving the admin with no durable export cursor.
  Suggested fix: turn export preparation into its own cursor-backed job phase: collect categories, words, media manifests, and `data.json` chunks incrementally, then write the zip batches from that manifest. Keep the admin-post fallback as a small-export-only path or redirect it into the same job.
  Suggested verification: add an export resource regression that seeds a large wordset/category set and asserts `ll_tools_start_export_bundle` returns after a bounded preparation chunk without calling the full payload builder; add coverage that no-JS export either uses the job path or refuses large payloads with a clear message.
- `chunk-22-export-import-b` [P2] Import preview builds external CSV/media bundles synchronously before a job can start.
  Evidence: `ll_tools_read_import_preview_from_zip()` opens and extracts the whole zip before returning preview data at `includes/admin/export-import.php:6810-6839`; when the bundle has no `data.json`, `ll_tools_import_read_payload_from_extract_dir()` delegates to `ll_tools_import_build_payload_from_external_csv_bundle()` at `includes/admin/export-import.php:6782-6796`. That CSV path recursively catalogs all image files at `includes/admin/export-import.php:5361-5416` and all audio files at `includes/admin/export-import.php:5450-5517`, reads each CSV into memory at `includes/admin/export-import.php:6104-6126`, loops every row into full `word_map`/`category_map` payload arrays at `includes/admin/export-import.php:6323-6577`, then returns the complete payload and media estimate at `includes/admin/export-import.php:6749-6769`. The media soft-limit warnings are calculated only after the payload exists at `includes/admin/export-import.php:4970-4988`, and the chunk-upload finish AJAX call still waits for this full preview preparation before returning at `includes/admin/export-import.php:7174-7191`.
  Likely impact: a large third-party CSV/media zip can spend the preview request extracting, scanning, parsing, and allocating the entire import payload before the durable batch importer is involved. The existing soft-limit warnings arrive too late to protect the server from time or memory pressure.
  Suggested fix: split external CSV preview into a manifest/scan job with hard entry, row, and byte caps before payload construction; stream CSV rows into bounded preview chunks and persist only small counts/samples until the user starts the import job.
  Suggested verification: add import-preview resource tests with many CSV rows and many media files that assert preview either refuses over-limit bundles or creates a bounded preview job without building the full `words` payload; cover the chunk-upload finish path so it returns a bounded status instead of waiting for full parsing.
- `chunk-22-export-import-b` [P2] Metadata update preview/apply processes the whole uploaded file synchronously.
  Evidence: `ll_tools_parse_metadata_updates_file()` reads the full CSV or JSON/JSONL file into memory at `includes/admin/export-import.php:7578-7590` and `includes/admin/export-import.php:7640-7655`, then accumulates every normalized row in arrays at `includes/admin/export-import.php:7613-7637` and `includes/admin/export-import.php:7688-7700`. Preview calls that parser and resolves every row/current value in one request at `includes/admin/export-import.php:8108-8357`. Confirmation reparses the same file and mutates every row in one admin-post request at `includes/admin/export-import.php:8421-8797`, then synchronously bumps/syncs every touched category at `includes/admin/export-import.php:8825-8839`. The exposed handlers are single nonce/capability-guarded POSTs at `includes/admin/export-import.php:8868-8917`.
  Likely impact: a large metadata update file can consume memory twice, run many post/meta reads during preview, then perform a long write request with partial-update risk and no durable cursor or resume point.
  Suggested fix: route metadata updates through the import job framework: store parsed row chunks during preview, expose status/result/readback, process a bounded number of rows per AJAX call, and defer category sync in bounded batches after writes.
  Suggested verification: extend `MetadataUpdateImportTest` or add a resource test with many metadata rows; assert preview/start stores chunks without reparsing the full file and each process call mutates at most the configured row limit while preserving undo and review-flag behavior.
- `chunk-23-export-import-c-admin-imports` [P2] STT training export builds and zips a whole wordset synchronously.
  Evidence: the admin-post handler for `ll_tools_export_stt_training_bundle` calls `@set_time_limit(0)`, then builds all entries and writes the zip before redirecting at `includes/admin/export-import.php:4697-4714`. The entry builder first loads all word IDs for the selected wordset at `includes/admin/export-import.php:12202-12204`, then fetches all matching `word_audio` posts with `posts_per_page => -1` at `includes/admin/export-import.php:12208-12215`, loops every word and recording at `includes/admin/export-import.php:12236-12315`, and the writer copies every audio file plus accumulates all CSV/JSONL metadata arrays before adding them to the zip at `includes/admin/export-import.php:12403-12533`.
  Likely impact: a production wordset with thousands of words and many recordings can turn one admin POST into a long CPU, memory, and disk-heavy request. Failures can leave the admin without a durable cursor or partial-result readback, and the current focused `SttTrainingExportTest` only covers small happy-path exports.
  Suggested fix: move STT export into the existing export job pattern or a dedicated cursor-backed job that pages word/audio IDs, writes metadata and audio entries incrementally, exposes status/result/download readback, and refuses or queues large synchronous fallback requests.
  Suggested verification: extend `SttTrainingExportTest` with a large wordset/recording fixture and assert the start request does not call the full-entry builder or run `posts_per_page => -1`; add process-step coverage proving each request copies only a bounded number of recordings.
- `chunk-23-export-import-c-admin-imports` [P2] Dictionary TSV import still front-loads the full upload before batching and has a synchronous POST fallback.
  Evidence: `ll_tools_dictionary_parse_tsv_file()` appends every parsed TSV row to `$rows` before returning at `includes/admin/dictionary-import-admin.php:90-165`. The AJAX start handler calls `ll_tools_dictionary_import_create_tsv_job_from_upload()` at `includes/admin/dictionary-import-admin.php:1633-1639`, which parses the full upload and then groups all rows before writing job chunks at `includes/admin/dictionary-import-admin.php:934-947` and `includes/admin/dictionary-import-admin.php:995-1004`. The same form still has a server-side POST fallback: `ll_tools_render_dictionary_import_page()` reparses the upload and calls `ll_tools_dictionary_import_rows()` directly at `includes/admin/dictionary-import-admin.php:1775-1793`, even though the form text says large imports run in short batches at `includes/admin/dictionary-import-admin.php:1886-1887`.
  Likely impact: a large TSV can consume memory and time before the resumable importer starts, and a no-JS/failed-JS/direct POST path can bypass the batch job entirely. That leaves the dictionary import manager vulnerable to timeouts and partial imports despite the progress UI.
  Suggested fix: stream TSV parsing into small group/chunk files during job creation, enforce row/byte caps before grouping, and make the non-JS POST path create or resume the same job and redirect to the status screen instead of calling the full synchronous importer.
  Suggested verification: add dictionary import resource tests with a large TSV that assert job creation writes bounded chunks without materializing all rows, and a direct POST regression proving `ll_tools_dictionary_import_rows()` is not called for normal admin form submissions.
- `chunk-24-ipa-keyboard-a` [P2] Transcription Manager symbol and letter-map tabs hydrate whole wordsets.
  Evidence: the symbol endpoint returns `ll_tools_ipa_keyboard_build_symbol_data()` directly at `includes/admin/ipa-keyboard-admin.php:8877-8885`, and the legacy combined data endpoint calls both symbol and letter-map builders at `includes/admin/ipa-keyboard-admin.php:8811-8814`. The symbol builder loads every word ID for the wordset at `includes/admin/ipa-keyboard-admin.php:1105-1107`, fetches every child `word_audio` with `posts_per_page => -1` at `includes/admin/ipa-keyboard-admin.php:1115-1121`, builds full recording payloads inside the complete loop at `includes/admin/ipa-keyboard-admin.php:1126-1155`, and returns every recording under each symbol at `includes/admin/ipa-keyboard-admin.php:1170-1175`. The letter-map path can rebuild the whole auto map at `includes/admin/ipa-keyboard-admin.php:1576-1578`, then separately collects samples by loading all word IDs and all recordings again at `includes/admin/ipa-keyboard-admin.php:1208-1226`. The JS renders all returned symbol recordings into details rows at `js/ipa-keyboard-admin.js:1627-1645`.
  Likely impact: opening the Symbols or Letter to IPA Map tab for a production wordset with thousands of words and many recordings can run full-wordset scans, hydrate image/category/internal-note payloads for many recordings, and duplicate recording rows across symbols. This makes a normal admin inspection path scale like a maintenance rebuild.
  Suggested fix: make symbol and letter-map endpoints summary-first: return counts plus a small sample, lazy-load recordings for one expanded symbol/letter with a paged endpoint, and move full map rebuilds behind an explicit bounded rebuild/status action or incrementally maintained cache.
  Suggested verification: extend `IpaKeyboardAdminAjaxTest` or add a resource-guard test that seeds a large wordset with many recordings, asserts tab-load endpoints do not use `posts_per_page => -1` over all words/recordings, and proves expanding one symbol fetches only a bounded page.
- `chunk-24-ipa-keyboard-a` [P2] Search pagination is applied after scanning every recording.
  Evidence: the client sends a bounded `per_page` value to `ll_tools_search_ipa_keyboard_recordings` at `js/ipa-keyboard-admin.js:1243-1253`, but the server first loads every word ID at `includes/admin/ipa-keyboard-admin.php:8421-8423` and every matching `word_audio` ID with `posts_per_page => -1` at `includes/admin/ipa-keyboard-admin.php:8376-8406`. `ll_tools_ipa_keyboard_search_recordings()` loops the full recording list at `includes/admin/ipa-keyboard-admin.php:8478-8573`, sorts all matches at `includes/admin/ipa-keyboard-admin.php:8575-8582`, and only then slices the current page at `includes/admin/ipa-keyboard-admin.php:8584-8589`. The same search helper also accepts raw `rx:` patterns and passes them to `preg_match()` for each scanned value at `includes/admin/ipa-keyboard-admin.php:7944-7962`, while the sanitizer only trims whitespace and does not cap query length at `includes/admin/ipa-keyboard-admin.php:166-171`.
  Likely impact: a search, review-only filter, issue filter, or regex pattern in the Transcription Manager can consume CPU and memory proportional to the entire wordset's audio inventory even when the UI shows 20 rows. A costly regex multiplies that full-scan behavior for any `view_ll_tools` user.
  Suggested fix: push pagination/filtering into query/index shape where possible, use a bounded candidate window for text/IPA scanning, and either remove raw regex search or add a strict length/complexity/time-budget guard with clear failure feedback.
  Suggested verification: add an integration resource test with hundreds or thousands of recordings that asserts `ll_tools_search_ipa_keyboard_recordings()` does not materialize all recording IDs before returning page 1; add a regression for overlong or pathological `rx:` input.
- `chunk-24-ipa-keyboard-a` [P2] Genç-Palu orthography rules auto-apply to every Zazaki wordset.
  Evidence: `ll_tools_ipa_orthography_get_profile_key()` returns `zazaki_genc_palu` whenever the wordset language is `zza`, `diq`, `kiu`, `zazaki`, or `zaza` and no explicit profile is stored at `includes/admin/ipa-keyboard-admin.php:3718-3727`. The only available profile is Genç-Palu-specific at `includes/admin/ipa-keyboard-admin.php:3738-3747`, and the default manual rules and optional matches are then hard-coded into the profile at `includes/admin/ipa-keyboard-admin.php:3751-3852` and `includes/admin/ipa-keyboard-admin.php:3865-3939`.
  Likely impact: a different Zazaki/Zaza wordset silently inherits Genç-Palu spelling rules, lexical assumptions, punctuation defaults, and contradiction checks just because its language code matches. That can produce wrong orthography suggestions or review warnings without the wordset opting into that regional profile.
  Suggested fix: require an explicit wordset/site setting for regional orthography profiles, keep the generic Zazaki language code profile-free by default, and provide a migration/admin notice for existing wordsets that relied on the implicit default.
  Suggested verification: add `IpaOrthographyConversionTest` coverage with two Zazaki wordsets: one without `ll_wordset_ipa_orthography_profile` that gets no Genç-Palu defaults, and one with the explicit profile that keeps the current Genç-Palu rules.

- `chunk-25-ipa-keyboard-b-admin-media` [P2] Audio/Image Matcher image picker loads every image in a category before rendering a small choice.
  Evidence: the admin page localizes only the initial category rows, but `ll_aim_get_images_handler()` fetches every published `word_images` post for the selected category with `posts_per_page => -1` at `includes/admin/audio-image-matcher.php:342-353`. It then builds complete thumbnail/image ID lists at `includes/admin/audio-image-matcher.php:355-366`, issues usage-count queries with one placeholder per image at `includes/admin/audio-image-matcher.php:370-409`, loops every image row at `includes/admin/audio-image-matcher.php:419-439`, and returns the complete array to the browser at `includes/admin/audio-image-matcher.php:440`. The current focused coverage verifies correctness for one image at `tests/Integration/AudioImageMatcherLazyLoadTest.php:79-125`, but does not assert page/window bounds.
  Likely impact: matching one audio item in an image-heavy category can hydrate and serialize hundreds or thousands of image posts and generate very large SQL `IN` lists before the user chooses one thumbnail. The `hide_used` toggle still pays the full category cost before filtering.
  Suggested fix: make `ll_aim_get_images_handler()` paged/searchable and return a capped window plus total/has-more metadata; compute usage counts only for the visible image IDs or back them with a compact indexed count; keep "hide used" in the query/index layer rather than filtering after full hydration.
  Suggested verification: extend `AudioImageMatcherLazyLoadTest` with a category containing many `word_images` and assert the image endpoint returns only the configured page size, includes `has_more`, and does not build usage-count SQL over the full category.
- `chunk-25-ipa-keyboard-b-admin-media` [P2] Missing Audio maintenance actions scan and rewrite all eligible posts synchronously.
  Evidence: `ll_run_missing_audio_scan()` scans every published eligible post with `posts_per_page => -1` at `includes/admin/missing-audio-admin-page.php:720-726` and reads each post body at `includes/admin/missing-audio-admin-page.php:730-743`. Regex preview and apply repeat the full-post scan at `includes/admin/missing-audio-admin-page.php:1014-1020` and `includes/admin/missing-audio-admin-page.php:1109-1115`, then run the user-supplied regex against each post at `includes/admin/missing-audio-admin-page.php:1033-1037` and `includes/admin/missing-audio-admin-page.php:1128-1132`. Table-header collection/preview/apply repeat the same unbounded query and parse each post DOM at `includes/admin/missing-audio-admin-page.php:1354-1360`, `includes/admin/missing-audio-admin-page.php:1502-1508`, and `includes/admin/missing-audio-admin-page.php:1634-1640`.
  Likely impact: a site with many public posts or long lesson/table content can turn one admin POST into a full-site content parse or full-site rewrite attempt. A broad or expensive regex multiplies the cost, and apply actions can update many posts in one request without cursor state or durable progress.
  Suggested fix: convert missing-audio scan, regex preview/apply, and table preview/apply into cursor-backed jobs with small post batches, saved progress, cancel/readback state, and strict regex length/shape limits. Keep the page render cheap and show the last scan/job status until the user starts or resumes a job.
  Suggested verification: add a Missing Audio admin resource test that seeds many eligible posts, starts scan/preview/apply with a small batch size, and asserts the first request processes only one bounded page and records remaining work; add an overlong or invalid regex guard regression.
- `chunk-25-ipa-keyboard-b-admin-media` [P2] Image Aspect Normalizer worklist and category row action rebuild all category aspect stats.
  Evidence: `ll_tools_aspect_normalizer_worklist_ajax()` loads all `word-category` IDs with `get_terms()` at `includes/admin/image-aspect-normalizer-admin.php:739-743`, then loops every category and calls `ll_tools_category_needs_aspect_normalization()` plus `ll_tools_get_category_image_aspect_stats()` at `includes/admin/image-aspect-normalizer-admin.php:748-777`. The taxonomy-list row action path also calls `ll_tools_get_aspect_normalization_needs_lookup()` at `includes/admin/image-aspect-normalizer-admin.php:1038`; that helper again loads all categories at `includes/admin/image-aspect-normalizer-admin.php:1005-1009` and checks every category at `includes/admin/image-aspect-normalizer-admin.php:1014-1025`. Each category stats cache miss loads all matching `words` with thumbnails and all `word_images` with thumbnails via `posts_per_page => -1` at `includes/lib/image-aspect.php:254-270` and `includes/lib/image-aspect.php:292-308`, then reads attachment dimensions for each usage at `includes/lib/image-aspect.php:423-455`.
  Likely impact: opening the normalizer page, or merely rendering the word-category admin list with row actions, can trigger an all-category image/aspect scan. On image-heavy sites this can make unrelated taxonomy admin screens slow and can stampede after the 10-minute per-category stats caches expire.
  Suggested fix: maintain a durable category-level aspect-needs index updated when category/image/thumbnail state changes, or refresh it through a bounded background job. The worklist and row actions should read cached/indexed status only and expose a "refresh status" action for explicit maintenance.
  Suggested verification: add an admin resource regression that seeds many categories with images, renders a word-category list row, and asserts it does not call `ll_tools_get_category_image_aspect_stats()` across every category; separately cover an explicit bounded refresh job.
- `chunk-25-ipa-keyboard-b-admin-media` [P2] Legacy Word Images fixer scans and creates every missing image post in one admin request.
  Evidence: `ll_find_words_missing_word_images()` queries every `words` post with a thumbnail using `posts_per_page => -1` at `includes/admin/word-images-fixer.php:17-25`, then loops every word and checks linked image state at `includes/admin/word-images-fixer.php:28-47`. The admin page calls that full scan on normal render at `includes/admin/word-images-fixer.php:75-81`, and the create action reruns the scan and loops all candidates through `ll_create_word_image_from_word()` in the same POST at `includes/admin/word-images-fixer.php:61-68`.
  Likely impact: opening the legacy cleanup page can scan the whole `words` table, and pressing the create button can create/update image records for every missing legacy word without a batch size, progress state, or partial-failure recovery. This is risky for older sites with many featured-image words.
  Suggested fix: make the fixer an explicit paged maintenance job: show a cheap cached count/status, process a bounded candidate page per request, store cursor/progress, and provide a dry-run/readback before creating records.
  Suggested verification: add a focused fixer test or maintenance-contract test that seeds many thumbnail words and asserts page render does not query all candidates and that create processes only a configured batch while preserving resume state.
- `chunk-26-offline-app-and-builder` [P2] Offline app export builds and zips the full app bundle in one admin request.
  Evidence: the `admin_post_ll_tools_export_offline_app` handler checks capability/nonce, then calls `ll_tools_build_offline_app_bundle()` and streams the zip in the same request at `includes/admin/offline-app-export.php:1270-1312`. The builder loads all selected categories at `includes/admin/offline-app-export.php:1346-1348`, loops every kept category and calls `ll_get_words_by_category()` before rewriting all words at `includes/admin/offline-app-export.php:1363-1393`, embeds the complete `offlineCategoryData` payload at `includes/admin/offline-app-export.php:1496-1557`, writes the entire data file at `includes/admin/offline-app-export.php:1797-1801`, copies every collected media asset at `includes/admin/offline-app-export.php:1811-1824`, and zips the staging directory recursively at `includes/admin/offline-app-export.php:1689-1690` and `includes/admin/offline-app-export.php:2387-2429`. The category and media helpers also include full reads such as all word IDs for all-category exports at `includes/admin/offline-app-export.php:2559-2576`, all mode words per category at `includes/admin/offline-app-export.php:2631-2634`, and all child audio posts per word at `includes/admin/offline-app-export.php:2036-2043`.
  Likely impact: exporting a production wordset with many categories, words, images, and recordings can spend one PHP request hydrating the full quiz dataset, resolving/copying media, encoding a large JS payload, and writing the zip. Timeouts or disk/memory failures can leave the admin without durable progress, resume, or partial-result readback.
  Suggested fix: move offline app export into a cursor-backed export job with explicit phases for category discovery, word/media manifest building, data-file writing, asset copy, and zip finalization. Keep direct streaming only below a small threshold or after a prepared bundle artifact exists.
  Suggested verification: extend `OfflineAppExportTest` with a large fixture and assert the initial export request creates/advances a bounded job instead of calling the full bundle builder; add batch-step coverage proving each step processes a capped number of categories, words, or assets and resumes safely after interruption.
## Resolved Findings

- 2026-07-10: Resolved the `chunk-18-dictionary-i18n-cache` P2 synchronous toolbar finding. Idle dictionary shortcodes now emit the existing lightweight deferred panel and skip letter/filter aggregation; active search, letter, filter, or explicit-scope URLs keep inline selected controls. The AJAX client retries `cache_warming` responses at most three times with server-directed delays clamped to 250ms-5s. Updated integration coverage proves idle SQL skips the available-letter group scan, and the 16-test deferred-toolbar browser suite covers one-time bootstrap, successful warming recovery, and the retry cap.
- 2026-07-10: Resolved the `chunk-08-wordset-pages-4` P2 unrelated settings prework finding. Settings data preparation is now explicit to the hub or active tool: offline options, template counts, upload shortcodes, recorder users/queues, managed-category rows, advanced settings, and transcription config are skipped elsewhere, and hub status cards are built only for the hub. Added a Visibility resource regression proving the cheap tool issues no recorder-user query or unbounded template-image query; the full settings UI suite retains hub and targeted-tool coverage.
- 2026-07-10: Resolved the `chunk-08-wordset-pages-4` P2 non-main content lesson finding. Content lesson queries, featured/mixed splitting, and mixed-card construction now run only for the main wordset view. Added a 20-lesson hidden-categories resource regression proving non-main rendering issues zero `ll_content_lesson` queries while existing main-view sequencing remains covered.
- 2026-07-10: Resolved the `chunk-14-audio-recorder-a` P2 default recorder bootstrap finding. The no-category shortcode path now discovers category terms through a compact wordset/category SQL relation, leaves unknown ready counts unlabeled, selects the start-word or first category, and loads only that category's existing bounded queue page. It no longer materializes the full word/image queue to choose a category, and an empty initial category can still render the selector for other categories. Added a 70-word/two-category shortcode regression proving only 10 initial items are localized and no unbounded `words` query runs.
- 2026-07-10: Resolved the `chunk-09-wordset-games-php-js-a` P2 Line Up launch finding. Line Up now counts all configured sequences from compact term metadata but hydrates at most three sequences per payload, with each sequence candidate-scoped to at most 24 configured words; both caps are filterable and hard-limited. Payloads report total availability separately from launch sequence count and mark truncated long sequences. Added a six-category resource regression proving a two-sequence/five-word test cap produces exactly two candidate-scoped hydrations while preserving all six in availability.
- 2026-07-10: Resolved the `chunk-09-wordset-games-php-js-a` P2 Unscramble launch finding. Launch now selects at most a capped candidate window (three times the launch cap, maximum 500), prioritizes progress IDs, uses bounded direct taxonomy queries for frontier fallback and aggregate availability, and hydrates only the candidate IDs through the existing candidate-scoped word path before exact tile/prompt validation. Added an 80-word resource regression proving a five-word launch hydrates no more than 15 candidate IDs while retaining the full availability count.
- 2026-07-10: Resolved the `chunk-04-user-progress-study` P2 deferred recommendation finding. A `defer_words` bootstrap now also defers recommendation refresh, reusing the bounded saved recommendation queue when one exists and otherwise returning an explicit `recommendation_refresh_deferred` marker for the existing loading-state recommendation request. It no longer invokes full renderable-category pool construction during bootstrap. Added regressions proving both empty-queue and saved-queue deferred bootstraps leave the full renderable-pool cache untouched.
- 2026-07-10: Resolved the `chunk-04-user-progress-study` P2 Learner Progress pagination finding. The report now intersects tracked progress users with WordPress users inside SQL, applies search/order/20-row pagination before hydration, and uses wordset-first user indexes in progress schema 1.3.2. Per-page progress stats now select only the columns required for studied/mastered/hard calculations. Added a 45-learner multi-page/search regression plus SQL-shape coverage for bounded page queries and explicit stats projection.
- 2026-07-10: Resolved the `chunk-16-flashcard-quiz-runtime-a` P2 standalone flashcard catalog finding. Unscoped `[flashcard_widget]` renders now process and localize at most 24 raw category candidates; explicit category shortcodes retain exact lookup behavior. A public, throttled, build-locked AJAX endpoint supplies subsequent bounded pages only when the learner uses the translated Load more control, with visible loading/error status and checked selections preserved. Added paged builder/AJAX resource coverage and a browser request/state regression.
- 2026-07-10: Resolved the `chunk-11-vocab-content-lessons` P2 related-content finding. Vocab lessons now issue a category-targeted, wordset-scoped lookup capped at six cards instead of hydrating every content lesson and filtering in PHP. Empty prerequisite lists no longer run a query, and nonempty prerequisite validation queries only the requested IDs. Added a 15-unrelated-lesson resource regression proving the complete card path has no unbounded content-lesson query.
- 2026-07-10: Resolved the `chunk-11-vocab-content-lessons` P2 public vocab-lesson print finding. Print availability and direct requests now run one exact-scope, ID-only `limit + 1` preflight with a filterable 200-word default and 500-word hard ceiling. Over-limit lessons hide print controls and direct print URLs return HTTP 413 before word or image hydration; the allowed print query is capped to the same limit. Added focused normal-path and over-limit resource coverage.
- 2026-07-10: Resolved the `chunk-13-word-grid-b` P2 lesson transcription/clear finding. Recording discovery now uses a direct taxonomy-scoped recording-ID cursor query that scans at most 26 candidates and returns at most 25 recordings per AJAX response; missing-only transcription filtering happens in SQL, and the browser continues across empty filtered pages while preserving cancellation and progress behavior. Clear actions use the same bounded cursor and defer IPA aggregate rebuilding until the final page. Added 60-recording integration coverage and Playwright regressions for transcription and clear cursor continuation.
- 2026-07-10: Resolved the `chunk-13-word-grid-b` P2 lesson bulk POS/grammar finding. Bulk metadata updates now use an ID-cursor query that scans at most 26 candidates and mutates at most 25 words per AJAX request; the browser continues under the existing saving state. Undo snapshots are sent and scope-validated in the same 25-row chunks without a full lesson query. Added a 60-word update/undo resource regression and browser coverage for cursor continuation and chunked undo.
- 2026-07-10: Resolved the `chunk-12-wordset-editor-word-grid-a` P2 deferred shell-card finding. Shell discovery now hard-clamps a filterable candidate window to at most 96 posts (24 by default), applies existing eligibility/order logic only to that bounded set, and hydrates at most six preview cards; the authoritative deferred grid remains unchanged. Added a 40-word resource regression proving bounded shell queries/six cards and full deferred response coverage, with the full deferred-grid browser suite green.
- 2026-07-10: Resolved the `chunk-12-wordset-editor-word-grid-a` P2 all-filtered bulk-action finding. Explicit page selections are capped at 75; all-filtered actions above that threshold create a user-scoped durable job without initial writes, then process locked ID-cursor batches of at most 25 with progress/readback and batch-sized undo history. Added an 82-word regression proving zero setup mutations, four bounded continuations, complete final state, and visible completion status.
- 2026-07-10: Resolved the `chunk-12-wordset-editor-word-grid-a` P2 filtered-view pagination finding. Search, category, uncategorized, image, recording, and non-title sort views now use parameterized SQL to count and page matching IDs before hydrating at most 75 display rows; filtered summary and category-option aggregates are separate queries. Added large filtered-page resource coverage plus exact/diacritic search and translation-backed page-boundary assertions.
- 2026-07-10: Resolved the `chunk-26-offline-app-and-builder` P3 offline export category-preload finding. The export page now renders only the wordset selector and an empty client cache; a nonce/capability-guarded AJAX endpoint loads categories for the selected valid wordset, with loading/failure states that keep export disabled until data arrives. Focused and full offline export coverage prove the initial HTML contains no category map and selected-wordset validation still works.
- 2026-07-10: Resolved the `chunk-21-export-import-a` P3 Recent Imports lesson-query finding. History rendering now shows at most 10 categories per entry and five matching lesson links per category, pushes imported wordset constraints into the bounded lesson query, disables found-row work, and reports the full category count plus truncation. Added a resource test proving four categories/five lessons become two `limit + 1` queries under test caps.
- 2026-07-10: Resolved the `chunk-23-export-import-c-admin-imports` P3 legacy translation migration finding. The maintenance action now selects eligible published word IDs with a bounded keyset SQL page, migrates at most 100 per request, and returns a nonce-backed continuation cursor plus batch/cumulative counts; the admin form switches to a continue state while rows remain. Added a 2/2/1 batch regression that preserves per-post capability checks.
- 2026-07-10: Resolved the `chunk-23-export-import-c-admin-imports` P2 unbounded pasted-list finding. Bulk word and prompt-audio imports now reject more than 200 actionable rows or 256 KB of text before category/post mutation, with filterable hard-clamped limits; prompt duplicate detection also stops after one matching ID. Focused regressions prove over-limit lists create neither content nor categories while normal imports remain green.
- 2026-07-10: Resolved the `chunk-22-export-import-b` P2 direct import fallback finding. The admin-post confirmation handler now shares the AJAX active-job gate, creates the durable import job, and redirects to the Import page where existing bootstrap code resumes processing; it no longer calls the full-zip importer. Updated the handler-flow regression to prove no content is created synchronously and that bounded job completion preserves preview cleanup, history, and undo.
- 2026-07-10: Resolved the `chunk-26-offline-app-and-builder` P2 uncapped Android PCM/STT finding. Offline speaking now rejects blobs over 8 MB and decoded audio over 15 seconds before base64 encoding; Android validates sample rate, channels, encoded size, decoded PCM bytes, and resampled samples before inference, with matching session and JNI sample ceilings. Added browser normal/oversized bridge coverage and JVM byte/sample budget tests; Java and all three native ABIs compile successfully.
- 2026-07-10: Resolved the `chunk-02-data-model-roles` P2 public vocab-lesson prompt-card detection finding. Initial lesson rendering now uses a shared category context and an ID-only prompt-card summary capped at three, with metadata and term cache priming disabled; full card expansion remains in the deferred grid request. Added a seven-card shell regression that verifies bounded query shape and three loading cards.
- 2026-07-10: Resolved the `chunk-18-dictionary-i18n-cache` P3 public linked-word count N+1 finding. Dictionary result pages now aggregate linked-word counts for visible entry IDs in one bounded SQL query and pass those counts into entry hydration, while direct single-entry callers retain the existing helper. Added a resource regression proving three visible cards use one aggregate count query and preserve zero/nonzero counts.
- 2026-07-10: Resolved the `chunk-14-audio-recorder-a` P2 new-word type-preflight scope finding. The preflight now sends current numeric and legacy wordset context, keys its cache by wordset and category, and the PHP handler rejects requests with no accessible explicit wordset instead of falling back to the default. Added focused browser request-contract and PHP strict-scope coverage.
- 2026-07-10: Resolved the `chunk-19-automation-rest-a` P2 missing-meta/bulk-update finding. Both routes now use bounded SQL ID scans before row hydration; sparse reads expose candidate offsets and writes persist a failure-safe `scan_after_id` cursor. Added sparse paging, resumable write, and legacy cap compatibility coverage.
- 2026-07-10: Resolved the `chunk-20-automation-rest-b-site-sync-cli` P2 REST report finding. `/report` now pages word IDs before building page-scoped coverage, exposes continuation metadata, and leaves full-wordset report generation to WP-CLI. Added a resource regression proving REST report pages do not run an unbounded words query.
- 2026-07-10: Resolved the `chunk-19-automation-rest-a` P2 REST category-scope finding. Category update and term-maintenance routes now use a shared aggregate category-ID helper that combines explicit ownership, scoped word relationships, and wordset-owned image relationships without hydrating all posts. Added legacy unowned-category resource coverage and reused the helper in Bulk Word Import.
- 2026-07-10: Resolved the `chunk-20-automation-rest-b-site-sync-cli` P2 transcription-validation candidate finding. Validation refreshes and job creation now select capped `word_audio` candidates through parent-word wordset joins and retain per-recording membership/staleness checks. Added focused cap, wordset-scope, and candidate-scope coverage.
- 2026-07-07: Resolved the `chunk-13-word-grid-b` P1 transcription remote-audio download finding. `ll_tools_resolve_audio_file_for_transcription()` now keeps same-site upload URLs local, rejects arbitrary remote URLs by default, requires explicit opt-in for remote transcription audio downloads, and reuses the private/reserved host and resolved-IP guards before `download_url()` can run. Added focused `SecurityHardeningRegressionTest` coverage for link-local URLs, public hostnames resolving to private IPs, and same-site upload URL resolution.
- 2026-07-07: Resolved the `chunk-15-audio-recorder-b-media-shortcodes` P2 bulk image upload scope finding. `[image_upload_form]` and `process_image_files` now require upload access, LL Tools access, and a real upload-management scope; non-global submissions are filtered to explicitly managed wordsets, empty non-global scope is rejected, and selected categories are validated against the chosen wordset scope. Added focused coverage for audio-recorder denial, manager-scoped rendering, and category-scope validation.
- 2026-07-07: Resolved the `chunk-20-automation-rest-b-site-sync-cli` P2 transcription and speaker update batch-cap finding. The transcription and word-audio speaker update routes now share exact-ID request normalization, reject over-limit arrays before mutation, expose cap metadata in `resource_guard`, and document the synchronous limits. Added focused `AutomationRestApiTest` coverage proving rejected over-cap payloads leave recording text, speaker metadata, and post authors unchanged.
- 2026-07-07: Resolved the `chunk-09-wordset-games-php-js-a` P2 speaking score/match guard finding. Speaking match scoring now rejects over-cap active candidate lists before scoring, reference-STT score and match AJAX paths share the existing user/IP speaking transcription rate-limit buckets before any cold reference STT can run, and focused `WordsetGamesTest` coverage proves rate-limited score requests and oversized match requests do not attempt external HTTP.
- 2026-07-08: Resolved the `chunk-27-docs-css-scripts` P2 stable release asset replacement finding. Stable publish now uploads a uniquely named temporary release asset, verifies its size, deletes the previous final-named zip only after the replacement is present, renames the verified replacement to the final asset name, and verifies the renamed asset. Added static `ReleasePluginScriptTest` coverage for the publish ordering and documented the safer replacement behavior in `RELEASING.md`.
- 2026-07-08: Resolved the `chunk-11-vocab-content-lessons` P2 corpus text grid default boundedness finding. `[ll_corpus_text_grid]` now defaults to a filterable bounded page size, keeps `limit="-1"` / `limit="all"` as explicit unbounded opt-ins, supports paged corpus grid requests, and renders pagination links when a collection has more texts than the current page. Added focused `ContentLessonCueParsingTest` coverage proving the default shortcode renders only the bounded page while page 2 and explicit unbounded requests still retrieve the expected texts.
- 2026-07-08: Resolved the `chunk-05-wordset-pages-1` P2 inactive-category preview GET mutation finding. Inactive preview card fallback links now point to the safe wordset page URL instead of a nonce-bearing action query, while the hidden POST form remains the JS-driven preview mutation path. The template redirect action handler now ignores GET inactive-category actions, so legacy preview URLs are read-only. Added focused `WordsetPageInactiveCategoryCardsTest` coverage proving rendered preview links do not expose action query strings and direct GET preview requests do not create image-backed draft words.

## Completed Chunks

- `chunk-28-tests-languages-i18n` completed 2026-07-07 15:20 Europe/Istanbul. Reviewed PHPUnit/Playwright test infrastructure, Local bootstrap scripts, live-smoke guards, performance fixture runner docs, tracked test helpers, public i18n manifest/source policy/tests, locale catalogs, and tier-2 DeepL/check/update scripts. No new findings were recorded; `php scripts/check-public-i18n.php --all-tier2 --json` confirmed the manifest matches the current POT selection and all active tier-2 locales cover the 646 public strings with compiled assets, while planned locales remain intentionally missing.

- `chunk-27-docs-css-scripts` completed 2026-07-07 15:09 Europe/Istanbul. Reviewed release packaging docs and scripts, AI context docs and generator contracts, maintenance source-contract Playwright spec, runtime asset manifest, top-level docs, CSS surfaces, git attributes/ignore rules, and command wrappers. Recorded one finding above for stable release asset replacement ordering; no source-code fix was made because the release workflow needs a deliberate mocked GitHub API test harness or transactional upload contract.

- `chunk-26-offline-app-and-builder` completed 2026-07-07 14:59 Europe/Istanbul. Reviewed offline app sync/auth/throttle handlers, offline export admin/bundle construction helpers, offline shell bootstrap and sync/STT bridge code, offline app builder scripts, Android/ JNI STT overrides, and focused offline export/sync/browser coverage. Recorded three findings above for synchronous full-bundle export, eager export-page category preload, and uncapped Android PCM/STT payload handling; no safe source-code fix was made because the fixes need a bounded export job contract, lazy category endpoint behavior, or Android bridge guard/test coverage.

- `chunk-25-ipa-keyboard-b-admin-media` completed 2026-07-07 14:52 Europe/Istanbul. Reviewed intervening locale-refresh commit `747bef95a67f788a596ae6b8f191bb365e9fe26a`, reconciled the heartbeat allowed HEAD per user instruction, then reviewed `includes/admin/ipa-keyboard-admin.php` lines 4501-8656, `js/ipa-keyboard-admin.js` lines 2701-5303, admin audio/image/media maintenance PHP, audio/image admin JS and CSS, `templates/audio-image-matcher-template.php`, and focused IPA/audio/image integration coverage. Recorded four findings above for the Audio/Image Matcher image picker, Missing Audio maintenance actions, Image Aspect Normalizer worklist/row-action scans, and the legacy Word Images fixer; no safe source-code fix was made because the fixes need bounded job or paged endpoint contracts.

- `chunk-01-core-runtime` completed 2026-07-07 00:28 Europe/Istanbul. Reviewed entry point/update hooks, bootstrap load order, asset enqueue/cache guards, template loader, flashcard shell, PHP compatibility helper, sort/text helpers, and plugin templates. No findings recorded for this chunk.
- `chunk-02-data-model-roles` completed 2026-07-07 00:28 Europe/Istanbul. Reviewed post types, smaller taxonomies, role helpers, privacy/login/class runtime, and related admin/public hook surfaces. Recorded three boundedness findings above; no safe source-code fix was made in this heartbeat because the fixes need lazy-loading or query-shape changes with focused tests.
- `chunk-03-wordset-category-taxonomies` completed 2026-07-07 00:34 Europe/Istanbul. Reviewed word-category and wordset taxonomy helpers, wordset isolation/health code, wordset template cloning helpers, wordset language settings, entity translations, word translation helpers, and relevant integration-test coverage. Recorded three boundedness findings above; no safe source-code fix was made in this heartbeat because the fixes need paged UI, durable jobs, or async maintenance flows with focused tests.
- `chunk-04-user-progress-study` completed 2026-07-07 00:50 Europe/Istanbul. Reviewed user progress persistence, study state, learner progress reporting, recommendation/analytics helpers, related AJAX handlers, and matching integration-test coverage. Recorded two boundedness findings above; no safe source-code fix was made in this heartbeat because the fixes need report query redesign and bounded recommendation refresh behavior with focused tests.
- `chunk-05-wordset-pages-1` completed 2026-07-07 00:50 Europe/Istanbul. Reviewed `includes/pages/wordset-pages.php` lines 1-4650, including early route helpers, cache/query helpers, category summaries, inactive-category actions, preview construction, and first-pass category row builders. Recorded one GET-mutation/resource finding above; no safe source-code fix was made in this heartbeat because the fix needs a POST/batch workflow and regression coverage.
- `chunk-06-wordset-pages-2` completed 2026-07-07 01:12 Europe/Istanbul. Reviewed `includes/pages/wordset-pages.php` lines 4651-9300, including lazy-card/category-search cache helpers, mixed-card rendering, wordset route URL helpers, manager settings/import/template actions, offline export action, and recorder/manager invite token helpers. No unresolved findings were recorded for this chunk. Fixed a safe inactive-card markup regression where preview-only inactive cards opened an anchor but closed a `div`, and added focused integration assertions for balanced inactive-card links.
- `chunk-07-wordset-pages-3` completed 2026-07-07 01:32 Europe/Istanbul. Reviewed `includes/pages/wordset-pages.php` lines 9301-13950, including manager/recorder invite acceptance, recorder access/queue POST and AJAX handlers, progress reset handling, teacher classes rendering, games config/i18n, front-end utility menu, settings hub/tool rendering, advanced settings payloads, category management helpers, and transcription settings UI. Recorded two boundedness findings above; no safe source-code fix was made in this heartbeat because the fixes need paged/searchable manager surfaces, aggregate counts, and batch/delete workflows with focused resource tests.
- `chunk-08-wordset-pages-4` completed 2026-07-07 01:51 Europe/Istanbul. Reviewed `includes/pages/wordset-pages.php` lines 13951-18591, `templates/wordset-page-template.php`, `css/wordset-pages.css`, focused `WordsetPage*` integration tests, and focused wordset-page/manager Playwright specs. Recorded three boundedness/resource findings above; no safe source-code fix was made in this heartbeat because the fixes need view-specific settings prework, cached/paged recorder queue summaries, and content-lesson render gating with resource tests.
- `chunk-09-wordset-games-php-js-a` completed 2026-07-07 02:12 Europe/Istanbul. Reviewed `includes/pages/wordset-games.php`, `css/wordset-games.css`, and `js/wordset-games.js` lines 1-5500, including game catalog rendering, practice/Unscramble/Line Up/speaking pool builders, launch and speaking AJAX handlers, early canvas/preload/runtime helpers, localized game-card markup, and focused Wordset Games test coverage. Recorded three resource/guard findings above; no safe source-code fix was made in this heartbeat because the fixes need bounded launch payload contracts, endpoint caps/rate guards, and focused resource tests.
- `chunk-10-wordset-games-js-b` completed 2026-07-07 02:31 Europe/Istanbul. Reviewed `js/wordset-games.js` lines 5501-12359, including the actual EOF because the manifest range ends at the stale line 11092, plus `tests/Integration/WordsetGamesTest.php` and `tests/e2e/specs/wordset-games-space-shooter.spec.js`. No new findings were recorded for this chunk; the notable speaking score/match guard gap remains tracked under the `chunk-09-wordset-games-php-js-a` findings above. No source-code fix was made in this heartbeat.
- `chunk-11-vocab-content-lessons` completed 2026-07-07 02:51 Europe/Istanbul. Reviewed vocab/content lesson page helpers, CPT/admin metadata handlers, lesson templates, lesson JS/CSS, focused integration tests, and focused Playwright specs. Recorded three boundedness/resource findings above; no safe source-code fix was made in this heartbeat because the fixes need bounded query contracts, pagination/preparation UX, and focused resource tests.
- `chunk-12-wordset-editor-word-grid-a` completed 2026-07-07 03:11 Europe/Istanbul. Reviewed `includes/pages/wordset-editor.php`, `css/editor-hub.css`, `js/editor-hub.js`, `includes/shortcodes/word-grid-shortcode.php` lines 1-5000 with adjacent shell-card call context, and `js/word-grid.js` lines 1-4500. Recorded three boundedness/resource findings above; no safe source-code fix was made in this heartbeat because the fixes need paged/indexed query contracts, bounded bulk-job behavior, or deferred shell contract changes with focused resource tests.
- `chunk-13-word-grid-b` completed 2026-07-07 03:47 Europe/Istanbul. Reviewed `includes/shortcodes/word-grid-shortcode.php` lines 5001-10619 because the manifest's 9560 end line is stale, `js/word-grid.js` lines 4501-10061 because the manifest's 9039 end line is stale, `js/word-edit-modal.js`, `js/bulk-category-edit.js`, `js/vocab-lesson-word-options-modal.js`, `css/vocab-lesson-word-options-modal.css`, focused `WordGrid*` integration tests, and `WordsetEditorToolTest`. Recorded three resource/security findings above; no safe source-code fix was made in this heartbeat because the fixes need bounded job/query contracts and URL validation coverage.
- `chunk-14-audio-recorder-a` completed 2026-07-07 03:51 Europe/Istanbul. Reviewed `includes/shortcodes/audio-recording-shortcode.php` lines 1-3500 with adjacent queue helper and request-context callsites, `js/audio-recorder.js` lines 1-2400 with adjacent new-word type preflight callsite, `css/recording-interface.css`, `includes/pages/recording-page.php`, focused recorder helper tests, and existing maintenance docs. Recorded two findings above; no safe source-code fix was made in this heartbeat because the fixes need a bounded recorder bootstrap contract and multi-wordset request-context coverage.
- `chunk-15-audio-recorder-b-media-shortcodes` completed 2026-07-07 04:22 Europe/Istanbul. Reviewed `includes/shortcodes/audio-recording-shortcode.php` lines 3501-7577 because the manifest's 6706 end line is stale, `js/audio-recorder.js` lines 2401-5294 because the manifest's 4670 end line is stale, `includes/shortcodes/word-audio-shortcode.php`, `includes/shortcodes/audio-credit-grid-shortcode.php`, upload helper PHP files, `js/word-audio*.js`, and focused audio recorder integration/E2E tests. Recorded one unresolved image-upload capability finding above. Fixed a scoped recorder guard so submitted `word_id` requests must belong to the requested recorder wordset before the upload/verify/skip flows can treat them as accessible uncategorized queue items; added focused helper coverage.
- `chunk-16-flashcard-quiz-runtime-a` completed 2026-07-07 04:32 Europe/Istanbul. Reviewed flashcard widget/quiz-page shortcode PHP, quiz page routing/sync/embed helpers, flashcard and quiz-page templates, flashcard base/mode CSS, quiz-page CSS, `js/quiz-pages*.js`, and the core flashcard `main.js`, `selection.js`, and `loader.js` runtime. Recorded three boundedness findings above; no safe source-code fix was made in this heartbeat because the fixes need bounded catalog/index/job contracts and focused resource tests rather than a narrow local patch.
- `chunk-17-flashcard-quiz-runtime-b` completed 2026-07-07 04:51 Europe/Istanbul. Reviewed flashcard mode/component JS, option-conflict helpers, Word Option Rules PHP/admin JS/CSS, and focused flashcard/option/listening/quiz browser and integration coverage. Recorded two boundedness findings above; no safe source-code fix was made in this heartbeat because the fixes need bounded editor and listening-prefetch contracts with focused resource tests.
- `chunk-18-dictionary-i18n-cache` completed 2026-07-07 05:14 Europe/Istanbul. Reviewed dictionary browser/search-index/snapshot/static-cache helpers, public static cache, dictionary and language-switcher shortcodes/assets, focused dictionary/static-cache integration tests, and the dictionary deferred-toolbar browser spec. Recorded three dictionary boundedness/query findings above; no safe source-code fix was made in this heartbeat because the fixes need deferred toolbar activation, cursor-backed snapshot/import phases, and batched linked-count contracts with focused resource tests.
- `chunk-19-automation-rest-a` completed 2026-07-07 05:31 Europe/Istanbul. Reviewed `includes/api/automation-rest.php` lines 1-5000, the shared CLI/editor helpers used by those REST paths, `AutomationRestApiTest`, `AutomationRestResourceGuardTest`, and REST automation docs. Recorded two boundedness findings above; no safe source-code fix was made in this heartbeat because the fixes need cursor-backed query contracts or a lightweight REST category-scope resolver with focused resource tests.
- `chunk-20-automation-rest-b-site-sync-cli` completed 2026-07-07 05:52 Europe/Istanbul. Reviewed `includes/api/automation-rest.php` lines 5001-10577 because the manifest's 9465 end line is stale, `includes/lib/site-sync.php`, `includes/admin/site-sync-admin.php`, `includes/cli/*.php`, Site Sync admin JS/CSS, local bin helpers, and `SiteSyncTest`. Recorded three REST boundedness findings above; no safe source-code fix was made in this heartbeat because the fixes need explicit REST caps, bounded candidate queries, or pagination/job contracts with focused resource tests.
- `chunk-21-export-import-a` completed 2026-07-07 06:11 Europe/Istanbul. Reviewed `includes/admin/export-import.php` lines 1-4500, `js/export-import-admin.js`, and `css/export-import-admin.css`, including export/import admin registration, export batching shell, import preview media streaming, import job bookkeeping, selector rendering, recent import summaries, and admin UI scripts. Recorded two export/import boundedness findings above. Fixed a malformed CSS fragment in `css/export-import-admin.css` that left an orphan declaration and extra closing brace after the mobile import-preview media rules.
- `chunk-22-export-import-b` completed 2026-07-07 06:31 Europe/Istanbul. Reviewed `includes/admin/export-import.php` lines 4501-9000, focused import/export integration tests, adjacent import-job/direct-import callsites, and the normal import JS submit interception. Recorded three boundedness/resource findings above; no safe source-code fix was made in this heartbeat because the fixes need preview/job/cursor contracts and focused resource tests rather than a narrow local patch.
- `chunk-23-export-import-c-admin-imports` completed 2026-07-07 06:51 Europe/Istanbul. Reviewed `includes/admin/export-import.php` lines 9001-13250, import/export job tail and STT export helpers, bulk word/translation import pages, dictionary import and source admin pages, prompt-audio import, settings/admin dashboard menu, dictionary import JS/CSS, bulk-word import JS, and the admin import preview/undo browser spec. Recorded four boundedness/resource findings above; no safe source-code fix was made in this heartbeat because the fixes need job/cursor contracts or deliberate row-cap behavior with focused resource tests.
- `chunk-24-ipa-keyboard-a` completed 2026-07-07 07:08 Europe/Istanbul. Reviewed `includes/admin/ipa-keyboard-admin.php` lines 1-4500, adjacent AJAX callsites needed to verify first-range helpers, `js/ipa-keyboard-admin.js` lines 1-2700, `css/ipa-keyboard-admin.css`, `css/ipa-fonts.css`, and focused IPA keyboard/search/orthography integration and browser coverage. Recorded three findings above covering whole-wordset symbol/map hydration, search pagination after full scans, and implicit Genç-Palu profile selection for all Zazaki wordsets; no safe source-code fix was made in this heartbeat because the fixes need bounded endpoint contracts or an explicit profile migration decision.
