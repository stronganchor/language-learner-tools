# Overnight Automated Code Review TODO

Started: 2026-07-06 23:38 Europe/Istanbul
Target branch: `dev`
Review base HEAD: `1ecfbeb3688dde36fac9917990189fed6b1f3406`
Cadence: once every 20 minutes until 2026-07-07 09:00 Europe/Istanbul

The chunk manifest and machine-local run state live at:

```text
C:\Users\messy\.codex\automation-state\ll-tools-overnight-code-review
```

## Operating Rules

- Review the next pending chunk from the manifest on each heartbeat.
- Treat this as a code-review pass: prioritize correctness, security, performance, large-wordset behavior, i18n readiness, capability/nonce checks, missing tests, and stale docs.
- Append follow-up items under `Findings TODO` with chunk id, severity, evidence, suggested fix, and suggested verification.
- Safe minor fixes may be made immediately, especially documentation, source-contract, typo, or narrow test-maintenance updates.
- Do not do broad refactors, live-site writes, release/version bumps, UI redesigns, or product behavior changes as part of the heartbeat unless a chunk reveals a small clear bug fix with focused verification.
- Commit completed repo changes after each heartbeat pass, following `AGENTS.md`.
- Stop instead of mutating if the branch or current allowed HEAD in the machine-local state no longer matches the checkout.

## Coverage Plan

The run is sized for 28 review chunks, which fits the remaining 20-minute slots before the 2026-07-07 09:00 Europe/Istanbul cutoff. Chunks cover tracked first-party source, docs, scripts, tests, styles, templates, locale sources, and first-party offline-app builder code. Vendor, generated artifacts, binary media, test reports, built release output, and the embedded third-party Whisper/GGML source tree are excluded from normal overnight coverage.

## Findings TODO

### 2026-07-10 whole-codebase audit follow-ups

6. **P2 - Recorder fallback paths can hydrate an entire wordset.** Evidence: `includes/shortcodes/audio-recording-shortcode.php` `ll_tools_recorder_get_word_ids_for_wordset_scope()` and the text-word query use `posts_per_page => -1` whenever no bounded candidate page is supplied; related image resolution then primes all returned words. Risk: page-load and popup delays plus PHP memory growth for production-size wordsets. Strategy: require cursor/page candidates throughout recorder bootstrap, including uncategorized and compatibility fallbacks, and return a loading/continuation state. Verification: large-wordset query-shape tests that fail on `-1` and browser coverage for page continuation. **Autonomy: safe with recorder regression coverage.**

9. **Resolved 2026-07-13 - Word-grid bulk changes now survive interruption with bounded durable rollback.** `includes/lib/word-grid-bulk-operations.php` stores an owner/scope/token operation plus one non-autoloaded before/expected-state chunk per bounded batch, with a one-day default expiry, lease-fenced state writes, crash-closed preparing/prepared phases, readback-verified writes/restores, conflict-safe reverse undo, and bounded scheduled chunk deletion. `includes/shortcodes/word-grid-shortcode.php` revalidates persisted targets before mutation and rejects browser-owned rollback payloads unless an explicit compatibility filter opts in; `js/word-grid.js` discovers interrupted state after reload and exposes Continue/Undo through a generic recovery row even when the original grammar control is disabled. Verification: `WordGridBulkEditStateTest` covers the 60-word bounded flow, owner isolation, status/reload resume, prepared-batch retry, current-scope revalidation, lease takeover, bounded cleanup, disabled-feature undo, recorded partial-write recovery, expiry cleanup, and post-write conflict skipping; `vocab-lesson-bulk-editor-mobile.spec.js` covers token continuation, reload controls, unavailable-feature recovery, reverse chunk undo, and hostile-theme layout.

10. **Resolved 2026-07-14 - Wordset isolation now uses a bounded, resumable, lease-fenced migration.** `includes/wordset-isolation.php` advances durable keyset phases through words, images, wordsets, option rules, lessons, users, and finalization; exact checkpoints persist before version publication and WP-CLI provides explicit completion/retry. Migration batches defer eager category-generated-page maintenance while preserving any enclosing queue. A durable locked coordinator waits for pre-existing quiz/vocab workers, tags fresh cursor-zero bounded passes, and retains retry intent through exact child completion. `WordsetIsolationMigrationTest` covers interruption, write loss, active-worker fencing, user-store preflight/CAS repair, final-state recovery, suppressed unbounded category aggregates, outer deferral preservation, owned/child scheduling failure, lock contention, and fresh reconciliation after an already-advanced worker.

11. **P2 - Offline app sessions use read-modify-write user-meta updates without concurrency control.** Evidence: `includes/offline-app-sync.php` create, authenticate-touch, and revoke operations each read the full session map and later call `update_user_meta()`. Risk: concurrent login, sync, and logout requests can overwrite one another, resurrect a revoked token, or drop a newly created session. Strategy: use one row per session or a compare-and-swap/lock around map updates; make revocation authoritative. Verification: interleaved concurrency regression covering create/touch/revoke and maximum-session trimming. **Autonomy: human review recommended for storage migration choice.**

12. **P2 - The full Turkish catalog is 373 current POT messages behind.** Evidence: WordPress PO parsing of `languages/ll-tools-text-domain.pot` versus `languages/ll-tools-text-domain-tr_TR.po` after this audit found 373 missing entries, concentrated in recent admin and maintenance workflows; the active public manifest remains complete. Risk: Turkish administrators see mixed English/Turkish interfaces. Strategy: translate in feature-sized batches under `languages/TURKISH_TRANSLATION_GUIDELINES.md`, compile `.mo` and `.l10n.php` each pass, and add a separately tracked admin-catalog coverage threshold. Verification: PO/POT key comparison, placeholder validation, glossary/tone searches, and Turkish browser smoke tests for each touched admin surface. **Autonomy: human language review required; mechanical extraction/compilation can be autonomous.**

13. **P3 - Teacher-class membership still uses full serialized ID arrays.** Evidence: `ll_tools_teacher_class_get_student_ids()` reads one serialized post-meta array; class counts deserialize it, selected-class assignment passes the complete array to `WP_User_Query::exclude`, and globally ordered progress paging passes the array to a bounded `WP_User_Query` include list. Risk: the admin and frontend now bound class/account/progress hydration, but a class with thousands of learners still creates large metadata payloads and SQL `IN`/`NOT IN` lists. Strategy: design a normalized membership table with indexed class/user rows, transactional dual-write/read migration, aggregate counts, and keyset progress/assignment queries; keep the existing meta contracts during a staged backfill and rollback window. Verification: migration rollback and consistency tests plus 10k-member query-size/order benchmarks. **Autonomy: human review required for the storage migration and ordering contract.**

15. **P3 - Authenticated progress snapshot requests permit up to 5,000 arbitrary positive word IDs.** Evidence: `includes/user-progress.php` `ll_tools_user_progress_sync_word_ids_limit()` defaults to 5,000; the response filters rows by requested wordset only after querying the user progress table. Risk: an authenticated learner can issue relatively expensive sparse-ID queries and recommendation refresh work. Strategy: lower the interactive cap, validate requested IDs against the active wordset in one bounded query, and paginate larger restoration flows. Verification: wrong-wordset and over-limit integration tests plus SQL placeholder/query-time assertions. **Autonomy: human review recommended for the client compatibility cap.**

16. **P3 - Public quiz catalog rendering still materializes and renders every catalog item.** Evidence: `ll_tools_quiz_pages_catalog_snapshot_items()` merges every durable chunk and sorts the complete array; the grid and dropdown shortcodes then render every item and cache the combined payload. Risk: chunked refresh protects cron/public cold-build work, but a catalog with thousands of generated quiz pages can still consume substantial request memory, transient space, and HTML/render time. Strategy: design paged grid hydration and a searchable/capped dropdown while preserving locale sorting, stale snapshots, popup links, and crawlable quiz-page discovery. Verification: 10k-page request-memory/HTML-size benchmark plus keyboard, no-JS, crawler, and locale-order browser coverage. **Autonomy: human review recommended because this changes public navigation behavior.**

## Resolved Findings

- 2026-07-13: Resolved the whole-codebase audit P3 unbounded legacy Classes administration. The wp-admin class list now uses 20-row plus-one paging and bounded title search; administrator teacher/learner options use a shared 25-row plus-one account search page; selected-class progress globally orders members and hydrates at most 25 users plus their aggregate progress, while clearly labeling page-scoped metrics and retaining the full learner count. Offset pages use deterministic `ID ASC` tie-breakers after title/display-name ordering. Native GET pagers preserve class/account/student state, reset empty class pages and clamp emptied final progress pages, and return mutations to the bounded view; off-page current/selected teachers remain assignable, and searched-empty classes have an accurate state. A query-observer regression proves class/account limits, two-user progress hydration under forced small page sizes, reverse-ID global ordering, class/student page clamping, all three continuation links, hidden off-page rows, and preserved action redirects. The deeper serialized-membership scaling concern remains item 13.
- 2026-07-13: Resolved the whole-codebase audit P2 synchronous inactive-category/vocab-lesson delete bypass. Both first-party entry points now create or continue one persisted wordset-level job, with a 25-item default and 100-item hard batch cap, state saved before mutation, a token-fenced renewable wordset lease plus a rollback-expiring marker in the previous per-category lock namespace, and transactional shared-map writes that lock and revalidate both lease rows. Remaining-row reconciliation covers failed writes or interrupted requests; one-off final-write failures self-recover after term deletion, and authenticated retries can finalize an already-missing term. Pending or failed jobs expose live Continue/Retry progress, contention without a category job remains an error instead of false progress, and a deleted current vocab lesson redirects errors to the safe wordset surface. Scoped term cleanup avoids scanning every enabled wordset. Focused PHP coverage proves bounded completion, old/new lock crossover and rollback protection, transactional lease fencing, failed-state recovery, missing-term finalization through both UI entry points, shared-map preservation, contention semantics, and current-lesson redirects; browser coverage proves a pending card remains actionable until the next batch completes.
- 2026-07-13: Resolved the whole-codebase audit P3 whole-catalog quiz refresh. Public cold reads retain stale-while-refresh behavior and never invoke the synchronous rebuild helper. Cron and nonce-backed warmup now advance one locked generation continuation: a canonical SQL keyset query prefers current quiz-page posts over legacy pages, deduplicates category shells before hydration, requests `batch + 1` rows (100 by default, hard cap 250), and builds category/gender metadata only for that batch. Accepted items are stored in generation-scoped chunks; the durable latest pointer switches atomically only after every post-type phase completes, replaced chunks remain discoverable across a publication interruption, scheduler resets serialize behind the scope lock, and workers fence writes against both lock tokens and durable generation ownership. Manifest readiness validates chunks with bounded-memory direct reads, and the public warmup budget permits up to 120 continuations by default. Resource regressions cover bounded chunks/queries, display ordering, no early publication, same-generation and interrupted-publication retries, corrupt-manifest recovery, current/legacy deduplication, and generation/lock takeover rejection; stale serving and lock contention remain covered.
- 2026-07-13: Resolved the whole-codebase audit P2 synchronous vocab-lesson Settings/Sync reconciliation. Settings, option updates, wordset edits, enable actions, and daily cron now only queue a persisted cleanup/sync state and schedule continuation; the legacy whole-site helper remains explicit compatibility/test code. Cleanup keysets active lesson IDs, sync discovers `LIMIT batch+1` category candidates for one enabled wordset, defaults to 10 items with a hard cap of 50, persists cursors/counters/failure state, and exposes running phase/progress plus retry UI. Shared word/prompt-card eligibility counts now use compact SQL `COUNT(DISTINCT ...)` aggregates instead of hydrating all relationship rows or all image-bearing word IDs. A two-wordset resource regression proves write-light start, `2/2/1` cleanup, per-wordset `2/1` category continuation, limited query shapes, completion, and failed-state restart; all 70 VocabLesson tests pass (1,103 assertions).
- 2026-07-13: Resolved the whole-codebase audit P1 Tier-2 public translation selection drift. Every mixed source now uses named PHP functions or strict anchor-bounded semantic regions; numeric line ranges were removed. The checker tokenizes PHP to resolve inclusive function ranges and fails on missing, duplicate, ambiguous, or reversed selectors. The reviewed 726-entry policy excludes Wordset Manager, recorder queue, staff review, and editing controls while restoring current learner auth, dictionary, interlinear, lesson, progress, word-grid, and game strings. A movement regression proves inserted lines do not change selection, and the DeepL updater now preserves supplemental source-backed PO entries. Turkish, German, and all eight active Tier-2 locales pass 726/726 coverage with validated placeholders and rebuilt `.mo`/`.l10n.php` assets.
- 2026-07-13: Resolved the whole-codebase audit P2 conservative upload image matching scan. A compact `ll_image_match_index` lookup table now stores exact, token, and character-trigram hashes for each `word_images` title, updates on image saves/deletes, and backfills through a locked ID-cursor cron job at 50 images per request by default with a hard cap of 200. Cold requests may advance only one 10-image inline batch, and each upload ranks at most 100 SQL-selected candidates by default (hard 250) before applying the existing confidence gate. Category and wordset-owner constraints are applied inside the candidate query before `LIMIT`, preserving typo-only fuzzy matches without hydrating every category image. Three resource tests (24 assertions) cover an 80-image typo-only match under `LIMIT 7`, owner filtering, and a `5 + 1` rebuild cursor.
- 2026-07-13: Resolved the whole-codebase audit P1 front-end manager offline export bypass. The manager form now creates the existing user-owned durable export job, advances only one bounded job step per non-JavaScript Continue request, and uses a dedicated front-end client for asynchronous progress, URL-token resume, transient request-error retry, and the existing nonce-protected download. Job loading verifies both the current owner and selected wordset, while unchecked custom categories fail closed instead of reverting to an all-category export. The focused suite passes 9 tests and 639 assertions, including manager start/continue/ownership coverage; two static browser tests cover completion/download and interrupted resume.
- 2026-07-13: Resolved the whole-codebase audit P2 Site Sync category snapshot hydration. `ll_tools_site_sync_collect_wordset_category_records()` now reuses the canonical relationship-level `ll_tools_get_word_category_ids_for_wordset_posts()` helper and loads only the distinct resulting category terms, eliminating the unbounded PHP word-ID array while preserving publish/draft/pending/private/future scope and isolated-category behavior. A 200-word resource regression proves one relationship query, no word `WP_Query`, and no cross-wordset leakage; all 31 Site Sync tests pass (287 assertions).
- 2026-07-13: Resolved the whole-codebase audit P1 public legacy-embed scan. `ll_tools_resolve_legacy_embed_isolated_category()` now uses a `terms.slug` prefix lookup joined to category ownership metadata, inspects 25 candidates by default with a hard cap of 100, and caches both positive and negative candidate lists against the category epoch. Existing wordset selection and quizzability checks remain in place. A 200-category resource regression proves an invalid slug issues one `LIMIT 7` lookup under a test cap and the repeated miss is served from cache (6 focused tests, 252 assertions).
- 2026-07-10: Resolved the `chunk-25-ipa-keyboard-b-admin-media` P2 Missing Audio maintenance finding. Normal renders now read only cached scan/header results and durable per-user job state. Scan, table-header discovery, regex preview, table preview, and fingerprint-guarded apply jobs process 10 post IDs per request by default with a hard cap of 50, expose continuation/cancellation and cumulative progress, and publish scan/header caches only after complete discovery. Apply jobs consume only the immediately preceding completed preview candidates and skip posts whose content changed. Regexes are capped at 512 bytes by default with a 1,024-byte hard ceiling, reject recursive subpatterns and invalid expressions, and execute under bounded PCRE backtrack/recursion limits. Added four resource tests covering bounded resume, render-time query shape, table jobs, changed-post protection, and unsafe regex rejection (90 assertions); the localization and word-audio capability suites also pass (17 assertions).
- 2026-07-10: Resolved the `chunk-26-offline-app-and-builder` P2 offline app export finding. The admin start request now creates only a durable user-owned job; capability/nonce-guarded step and download endpoints advance explicit category discovery, word/media manifest, asset copy, streamed data, and incremental zip phases. Defaults/hard caps are `5/25` categories, `20/100` words, `25/100` media assets, `25/100` data rows, and `50/200` zip entries per request. Jobs use locked manifests, owner/expiry checks, recoverable resume URLs, translated phase/loading/error state, and preserve the existing full builder as an explicit compatibility helper. A three-category/15-word resource regression proves start never invokes that builder, SQL requests only `limit + 1`, every phase respects test caps, and the final `offline-data.js` is valid and complete. All eight offline export tests (615 assertions) and 22 capability tests (50 assertions) pass. The focused browser progress spec was safely skipped by its local environment gate in this rerun.
- 2026-07-10: Resolved the `chunk-18-dictionary-i18n-cache` P2 snapshot import backup/cleanup finding. Snapshot jobs now expose durable `backup`, `import`, `cleanup`, and `complete` phases. Start creates only job/chunk/index state; backup uses bounded ID-cursor batches and chunk manifests, then Undo consumes those chunks directly. Override imports build a bucketed index of every incoming key before writes, including invalid/failed rows, and only after all import chunks finish do they keyset-scan and delete missing existing entries in bounded cleanup batches. Progress/status readback covers every phase; backup I/O retains the established warning-and-continue policy, while manual full exports remain explicit. Added a 2,000-entry 40-row backup regression and 1,000-entry 75-row override cleanup/resume regression; all 89 dictionary tests (3,891 assertions) pass. The local browser import/undo spec was safely skipped because its environment gate was unavailable.
- 2026-07-10: Resolved the `chunk-17-flashcard-quiz-runtime-b` P2 Word Option Rules editor finding. Normal renders now expose searchable pages of 25 category words (hard 50), hydrate audio and automatic text/image conflict reasons only for that page, page existing manual pairs at 25, and use a nonce/capability-guarded 20-result candidate search for pair selection. Group autosaves submit visible candidate IDs and merge only that page's membership delta, preserving off-page groups; pair add/remove checks hydrate only the named two words and retain existing automatic conflict locks, recording-type scopes, image-similarity overrides, and import/export maintenance semantics. Added 80-word render, group-delta, and pair-lock resource regressions; eight focused PHP tests (62 assertions) and all five mobile/autosave browser tests pass.
- 2026-07-10: Resolved the `chunk-22-export-import-b` P2 metadata update preview/apply finding. Preview now reads and resolves only a 25-row sample (hard 100) and clearly labels partial counts. Confirmation creates a durable user-owned job without parsing or mutating rows; each nonce/capability-guarded Continue step streams at most 25 normalized CSV/JSONL rows by byte offset (hard 100), persists progress/result/undo state, and bounds category maintenance to that row batch. Large JSON arrays receive explicit 2 MiB/500-row ceilings and guidance to use streamable CSV/JSONL. Source size/mtime checks and a process lock protect resume safety. Cumulative changed-ID and undo state preserve unique counts, sequential duplicate-row semantics, and the original pre-job value for Undo. Added a `2/2/1` five-row duplicate-target regression (31 assertions); all 10 metadata import tests (100 assertions) and six import-history/undo tests (74 assertions) pass.
- 2026-07-10: Resolved the `chunk-22-export-import-b` P2 external CSV/media preview finding with pre-hydration hard guards. Before creating an extraction directory, both direct and chunk-upload-finish previews now validate the central zip manifest, distinguish native `data.json` bundles, cap external bundles at 32 top-level CSV files, 5,000 streamed data rows, 16 MiB of CSV, 1 MiB per physical CSV row, 2,000 media files, and 512 MiB of media, with filterable hard ceilings. Row counting stops as soon as the cap is crossed, so refused bundles never recurse media directories or build category/word payload maps. Added row- and media-cap regressions; all 16 external CSV tests (371 assertions, one environment-dependent encoding skip) and 11 AJAX import-flow tests (230 assertions) pass.
- 2026-07-10: Resolved the `chunk-24-ipa-keyboard-a` P2 symbol/letter-map hydration finding. The tabs now read compact term-level occurrence, recording-count, sample-ID, and letter-map aggregates; each summary hydrates at most 24 sample recordings globally and no longer queries all words or recordings. Expanding one symbol or letter scans at most 250 scoped recording candidates and returns at most 20 full rows with cursor continuation. Aggregate creation is an explicit edit-capability-guarded job with a 100-recording default and 250 hard cap per step, generation checks that discard dirty results, stale invalidation on relevant recording/word changes, and translated rebuild progress/error states. Added `3/6/8` rebuild and `2/2/empty` expansion resource coverage; all 14 IPA AJAX tests (258 assertions), six IPA sort tests (64 assertions), and 15 Transcription Manager browser tests pass.
- 2026-07-10: Resolved the `chunk-16-flashcard-quiz-runtime-a` P2 quiz-page cold catalog finding. Grid and dropdown reads now use an epoch-, wordset-, locale-, and viewer-scoped materialized snapshot. Cold anonymous renders issue no quiz-page/category catalog query, queue the locked WP-Cron worker, and expose a translated loading state that starts a nonce-backed asynchronous warmup under per-scope and global build locks; the browser retries at most eight times and reloads only after the current snapshot is ready. Stale snapshots remain immediately usable during rebuilds and lock contention, but explicit wordset snapshots are rejected when their stored IDs no longer resolve to the requested scope, and readiness requires the pending epoch cache key. Hot reads remain write-free. Existing eligibility, visibility, wordset scope, CPT/legacy deduplication, sorting, links, gender metadata, popup payloads, and display modes remain in the worker-built catalog. Six catalog tests (120 assertions), two wordset-query tests (40 assertions), the translation regression (10 assertions), the mocked browser warmup test, and all five previously failing cold/throttled quiz browser workflows pass.
- 2026-07-10: Resolved the `chunk-08-wordset-pages-4` P2 recorder queue overview finding. The overview now pages assigned recorders at six and categories at eight by default, hard-caps each page at 20, and renders per-recorder/category cached summaries. A shared eight-step refresh budget advances each missing or stale summary through at most two bounded raw candidate chunks per step; incomplete mostly-completed/hidden categories persist scan offsets and expose Continue rather than scanning to exhaustion. The focused detailed queue keeps its existing pagination and hidden/completed semantics. Added recorder/category page coverage and a 100-hidden-word regression proving the first request stops after two 40-row scans and the next resumes the remainder; the three-test resource suite (70 assertions), 36 settings tests (581 assertions), 19 recorder helper tests (169 assertions), and four category privacy tests (67 assertions) pass.
- 2026-07-10: Resolved the `chunk-21-export-import-a` P2 export batch-start finding. Export start now creates only durable job, manifest, and staging state; locked continuation requests page template images, regular images, and words at 25 rows by default with a 100-row hard cap, stream `data.json` incrementally, and write media from a line-oriented attachment manifest under item, byte, and time budgets. The non-JavaScript fallback creates and continues the same resumable job instead of building or streaming the bundle synchronously. Added a resource regression proving start performs no post query and 12 payload records prepare across `5/5/remaining` image and word pages; the four-test export suite (175 assertions), template bundle suite (61 assertions), and five-test STT export suite (177 assertions) pass.
- 2026-07-10: Resolved the `chunk-23-export-import-c-admin-imports` P2 dictionary TSV front-load finding. Uploads now stream one TSV row at a time into 64 bounded hash buckets, then build durable chunks without splitting a normalized headword group; this preserves replacement-sense semantics without retaining the upload or grouped dataset in memory. Hard ceilings cover 128 MiB files, 250,000 rows, raw/prepared rows, bucket/spool bytes, 10,000-row/4 MiB headword groups, source definitions, and 4 MiB chunks. Error paths remove temporary state. The direct POST fallback creates/resumes the same durable job instead of calling the synchronous importer. Added stream/group, hard-limit, and fallback regressions (50 assertions); all 87 Dictionary feature tests (838 assertions) pass. Snapshot backup/override cleanup remains tracked separately.
- 2026-07-10: Resolved the `chunk-03-wordset-category-taxonomies` P2 Line-Up settings finding. Taxonomy and vocab-lesson settings now render only stored direction/config metadata and initialize a nonce/capability-guarded manager that pages both the custom prefix and searchable category candidates at 25 rows, hard-capped at 50. Add/reset/move operations validate only the affected word; explicit replacement submissions are capped at 500 and candidate-scoped. Autosaves cannot truncate an off-page sequence. The runtime appends every non-custom category word in automatic order, so resetting a word repositions rather than excludes it. Added four manager tests (46 assertions), retained 10 vocab settings tests (112 assertions) and four game-level Line-Up tests (66 assertions), and added four passing browser settings tests.
- 2026-07-10: Resolved the `chunk-07-wordset-pages-3` P2 wordset category settings finding. The Categories tool now renders searchable pages of 20 owned terms with a hard cap of 100, uses aggregate term summaries for the settings hub, and performs one grouped lesson-count query for only the visible category IDs. Opening Categories skips unrelated full study/category payload preparation. Category deletion is a locked durable job that removes at most 25 linked lessons/words per request, persists progress and failure readback, and exposes Continue/Retry controls while preserving words and existing ownership/capability/nonce checks. Added a 26-category page/search regression and a six-linked-item `2/2/2` deletion regression; the new two-test suite (113 assertions), 36 settings tests (581 assertions), and 11 inactive-category tests (260 assertions) pass.
- 2026-07-10: Resolved the `chunk-24-ipa-keyboard-a` P2 search pagination finding. Empty-query and review-only views now use taxonomy-scoped SQL counts plus ordered `LIMIT` pages; text, regex, and stale-issue inspection scan only one hard-capped candidate page per request and continue through the existing Load more control with honest partial-total copy. Search responses derive keyboard inventory only from the bounded visible reviewed rows, so they no longer trigger the whole-wordset reviewed-symbol scan. Queries are capped at 256 characters; regex bodies are capped at 128, recursive forms are rejected, and accepted PCRE patterns carry match/depth limits with translated errors. Added seven-recording `3/3/1` resource coverage proving bounded continuation and zero unbounded recording-ID queries, plus unsafe-regex coverage; all 12 IPA AJAX tests and 14 Transcription Manager browser tests pass.
- 2026-07-10: Resolved the `chunk-03-wordset-category-taxonomies` P2 Wordset Isolation health finding. Admin notices now read only cached report and durable job state; a missing/stale report shows a nonce- and maintenance-capability-guarded Check now action that queues the existing collectors on WP-Cron. The five-minute `admin_init` auto-repair scan is removed. Vocab-lesson repair is now an explicit ID-cursor job capped at 100 lessons per cron step, with progress/readback and a fresh health check queued after completion. The report TTL is one day. Added cache-miss notice coverage proving zero collector queries and a five-lesson `2/2/1` repair regression.
- 2026-07-10: Resolved the `chunk-02-data-model-roles` P2 Dictionary admin filter finding. The Words list now renders a translated bounded autocomplete instead of every dictionary entry. Dictionary-entry wordset, POS, source, dialect, type, review, and linked-status filters compile to SQL `EXISTS` predicates over existing scope/relationship data; lookup index version 5 adds exact source/dialect rows through the existing cursor rebuild, with normalized search-meta fallback while that rebuild is pending. Visible linked counts use one aggregate query and the list column links to the filtered Words screen without per-row previews. Added three resource tests covering 30-entry render boundedness, all filter dimensions including legacy linked-scope fallback, exact index rows, and zero per-row count queries, plus browser autocomplete/stale-ID coverage.
- 2026-07-10: Resolved the `chunk-02-data-model-roles` P2 Content Lesson editor preload finding. Edit screens now localize only a bounded 40-row page for the active wordset instead of complete maps for every wordset. A nonce- and `view_ll_tools`-guarded endpoint hard-caps category and prerequisite pages at 75 rows, selected values remain present outside the page, and candidate-scoped SQL validation replaces category-wide save scans. The editor exposes translated search, loading/error state, and Load more controls with stale wordset-response rejection. Added active-wordset payload, bounded page/selected-row, and browser wordset/search/continuation coverage.
- 2026-07-10: Resolved the `chunk-07-wordset-pages-3` P2 Teacher Classes preload finding. Guest and unauthorized views no longer query available wordsets. Authorized front-end views fetch searchable pages of at most 20 classes; administrators fetch searchable pages of at most 25 teacher/learner accounts; and selected-class progress hydrates at most 25 learners per page. Translated previous/next controls retain the active class and filters, and wordset filtering now runs in the bounded class query instead of after full hydration. Added focused class/account page-search and 26-learner progress pagination coverage; the complete Teacher Classes and Wordset Games suites remain green.
- 2026-07-10: Resolved the `chunk-18-dictionary-i18n-cache` P2 synchronous toolbar finding. Idle dictionary shortcodes now emit the existing lightweight deferred panel and skip letter/filter aggregation; active search, letter, filter, or explicit-scope URLs keep inline selected controls. The AJAX client retries `cache_warming` responses at most three times with server-directed delays clamped to 250ms-5s. Updated integration coverage proves idle SQL skips the available-letter group scan, and the 16-test deferred-toolbar browser suite covers one-time bootstrap, successful warming recovery, and the retry cap.
- 2026-07-10: Resolved the `chunk-08-wordset-pages-4` P2 unrelated settings prework finding. Settings data preparation is now explicit to the hub or active tool: offline options, template counts, upload shortcodes, recorder users/queues, managed-category rows, advanced settings, and transcription config are skipped elsewhere, and hub status cards are built only for the hub. Added a Visibility resource regression proving the cheap tool issues no recorder-user query or unbounded template-image query; the full settings UI suite retains hub and targeted-tool coverage.
- 2026-07-10: Resolved the `chunk-08-wordset-pages-4` P2 non-main content lesson finding. Content lesson queries, featured/mixed splitting, and mixed-card construction now run only for the main wordset view. Added a 20-lesson hidden-categories resource regression proving non-main rendering issues zero `ll_content_lesson` queries while existing main-view sequencing remains covered.
- 2026-07-10: Resolved the `chunk-14-audio-recorder-a` P2 default recorder bootstrap finding. The no-category shortcode path now discovers category terms through compact word and published prompt-card wordset/category SQL relations, leaves unknown ready counts unlabeled, selects the start-word or first category, and loads only that category's existing bounded queue page. It no longer materializes the full word/image/prompt queue to choose a category, prompt-only wordsets no longer fall through to an empty uncategorized queue, and an empty initial category can still render the selector for other categories. The 70-word/two-category regression proves only 10 initial items are localized and no unbounded `words` query runs; a prompt-only no-category regression and the real REST-backed prompt-card browser fixture also pass.
- 2026-07-10: Resolved the `chunk-09-wordset-games-php-js-a` P2 Line Up launch finding. Line Up now counts all configured sequences from compact term metadata but hydrates at most three sequences per payload, with each sequence candidate-scoped to at most 24 configured words; both caps are filterable and hard-limited. Payloads report total availability separately from launch sequence count and mark truncated long sequences. Added a six-category resource regression proving a two-sequence/five-word test cap produces exactly two candidate-scoped hydrations while preserving all six in availability.
- 2026-07-10: Resolved the `chunk-09-wordset-games-php-js-a` P2 Unscramble launch finding. Launch now selects at most a capped candidate window (three times the launch cap, maximum 500), prioritizes progress IDs, uses bounded direct taxonomy queries for frontier fallback and aggregate availability, and hydrates only the candidate IDs through the existing candidate-scoped word path before exact tile/prompt validation. Added an 80-word resource regression proving a five-word launch hydrates no more than 15 candidate IDs while retaining the full availability count.
- 2026-07-10: Resolved the `chunk-04-user-progress-study` P2 deferred recommendation finding. A `defer_words` bootstrap now also defers recommendation refresh, reusing the bounded saved recommendation queue when one exists and otherwise returning an explicit `recommendation_refresh_deferred` marker for the existing loading-state recommendation request. It no longer invokes full renderable-category pool construction during bootstrap. Added regressions proving both empty-queue and saved-queue deferred bootstraps leave the full renderable-pool cache untouched.
- 2026-07-10: Resolved the `chunk-04-user-progress-study` P2 Learner Progress pagination finding. The report now intersects tracked progress users with WordPress users inside SQL, applies search/order/20-row pagination before hydration, and uses wordset-first user indexes in progress schema 1.3.2. Per-page progress stats now select only the columns required for studied/mastered/hard calculations. Added a 45-learner multi-page/search regression plus SQL-shape coverage for bounded page queries and explicit stats projection.
- 2026-07-10: Resolved the `chunk-25-ipa-keyboard-b-admin-media` P2 Image Aspect Normalizer discovery finding. Explicit worklist requests now refresh a filterable page of eight category statuses, persist counts/need state against the existing per-category cache version, return continuation metadata, and expose translated Load more progress. Taxonomy row actions read only the current term's valid status; stale/missing status causes no action and no stats query. Added seven-category resource coverage proving a three-category page performs only six scoped stats queries and row actions perform zero, plus browser continuation coverage.
- 2026-07-10: Resolved the `chunk-25-ipa-keyboard-b-admin-media` P2 legacy Word Images fixer finding. Normal page renders no longer scan candidates. The nonce/capability-guarded maintenance form now scans at most 26 thumbnail words and creates from at most 25 per POST, continues with an ID cursor, preserves cumulative created/failure counters in per-user durable state, and reads that state without scanning when the admin returns. Added a seven-word 3/3/1 regression proving bounded query shape, resume readback, complete creation, and zero render-time scans.
- 2026-07-10: Resolved the `chunk-25-ipa-keyboard-b-admin-media` P2 Audio/Image Matcher picker finding. The image endpoint now selects an ordered ID page with a filterable 48-row default and hard 96-row ceiling, returns continuation metadata, hydrates only visible posts, and limits usage-count `IN` lists to that page. Hide-used mode applies linked-image and thumbnail `NOT EXISTS` checks before pagination. The admin grid exposes translated loading/error state and Load more continuation. Added 8-image endpoint/query-shape coverage and a browser continuation regression.
- 2026-07-10: Resolved the `chunk-17-flashcard-quiz-runtime-b` P2 Listening category-queue finding. Listening now keeps a filterable window of at most three unresolved category loads, advances it as earlier categories arrive, and retains estimated progress without queueing the full selection. Relaunch, mode switch, and close invalidate old request tokens; the shared loader drops invalid queued work before AJAX and ignores stale in-flight responses. Added 30-category window/advance/cancel, mode-switch/close, and loader cancellation browser regressions.
- 2026-07-10: Resolved the `chunk-16-flashcard-quiz-runtime-a` P2 standalone flashcard catalog finding. Unscoped `[flashcard_widget]` renders now process and localize at most 24 raw category candidates; explicit category shortcodes retain exact lookup behavior. A public, throttled, build-locked AJAX endpoint supplies subsequent bounded pages only when the learner uses the translated Load more control, with visible loading/error status and checked selections preserved. Added paged builder/AJAX resource coverage and a browser request/state regression.
- 2026-07-10: Resolved the `chunk-11-vocab-content-lessons` P2 related-content finding. Vocab lessons now issue a category-targeted, wordset-scoped lookup capped at six cards instead of hydrating every content lesson and filtering in PHP. Empty prerequisite lists no longer run a query, and nonempty prerequisite validation queries only the requested IDs. Added a 15-unrelated-lesson resource regression proving the complete card path has no unbounded content-lesson query.
- 2026-07-10: Resolved the `chunk-11-vocab-content-lessons` P2 public vocab-lesson print finding. Print availability and direct requests now run one exact-scope, ID-only `limit + 1` preflight with a filterable 200-word default and 500-word hard ceiling. Over-limit lessons hide print controls and direct print URLs return HTTP 413 before word or image hydration; the allowed print query is capped to the same limit. Added focused normal-path and over-limit resource coverage.
- 2026-07-10: Resolved the `chunk-13-word-grid-b` P2 lesson transcription/clear finding. Recording discovery now uses a direct taxonomy-scoped recording-ID cursor query that scans at most 26 candidates and returns at most 25 recordings per AJAX response; missing-only transcription filtering happens in SQL, and the browser continues across empty filtered pages while preserving cancellation and progress behavior. Clear actions use the same bounded cursor and defer IPA aggregate rebuilding until the final page. Added 60-recording integration coverage and Playwright regressions for transcription and clear cursor continuation.
- 2026-07-10: Resolved the `chunk-13-word-grid-b` P2 lesson bulk POS/grammar finding. Bulk metadata updates now use an ID-cursor query that scans at most 26 candidates and mutates at most 25 words per AJAX request; the browser continues under the existing saving state. Undo snapshots are sent and scope-validated in the same 25-row chunks without a full lesson query. Added a 60-word update/undo resource regression and browser coverage for cursor continuation and chunked undo.
- 2026-07-10: Resolved the `chunk-12-wordset-editor-word-grid-a` P2 deferred shell-card finding. Shell discovery now hard-clamps a filterable candidate window to at most 96 posts (24 by default), applies existing eligibility/order logic only to that bounded set, and hydrates at most six preview cards; the authoritative deferred grid remains unchanged. Added a 40-word resource regression proving bounded shell queries/six cards and full deferred response coverage, with the full deferred-grid browser suite green.
- 2026-07-10: Resolved the `chunk-12-wordset-editor-word-grid-a` P2 all-filtered bulk-action finding. Explicit page selections are capped at 75; all-filtered actions above that threshold create a user-scoped durable job without initial writes, then process locked ID-cursor batches of at most 25 with progress/readback and batch-sized undo history. Added an 82-word regression proving zero setup mutations, four bounded continuations, complete final state, and visible completion status.
- 2026-07-10: Resolved the `chunk-12-wordset-editor-word-grid-a` P2 filtered-view pagination finding. Search, category, uncategorized, image, recording, and non-title sort views now use parameterized SQL to count and page matching IDs before hydrating at most 75 display rows; filtered summary and category-option aggregates are separate queries. Added large filtered-page resource coverage plus exact/diacritic search and translation-backed page-boundary assertions.
- 2026-07-10: Resolved the `chunk-26-offline-app-and-builder` P3 offline export category-preload finding. The export page now renders only the wordset selector and an empty client cache; a nonce/capability-guarded AJAX endpoint loads categories for the selected valid wordset, with loading/failure states that keep export disabled until data arrives. Focused and full offline export coverage prove the initial HTML contains no category map and selected-wordset validation still works.
- 2026-07-10: Resolved the `chunk-21-export-import-a` P3 Recent Imports lesson-query finding. History rendering now shows at most 10 categories per entry and five matching lesson links per category, pushes imported wordset constraints into the bounded lesson query, disables found-row work, and reports the full category count plus truncation. Added a resource test proving four categories/five lessons become two `limit + 1` queries under test caps.
- 2026-07-10: Resolved the `chunk-23-export-import-c-admin-imports` P3 legacy translation migration finding. The maintenance action now selects eligible published word IDs with a bounded keyset SQL page, migrates at most 100 per request, and returns a nonce-backed continuation cursor plus batch/cumulative counts; the admin form switches to a continue state while rows remain. Added a 2/2/1 batch regression that preserves per-post capability checks.
- 2026-07-10: Resolved the `chunk-23-export-import-c-admin-imports` P2 unbounded pasted-list finding. Bulk word and prompt-audio imports now reject more than 200 actionable rows or 256 KB of text before category/post mutation, with filterable hard-clamped limits; prompt duplicate detection also stops after one matching ID. Focused regressions prove over-limit lists create neither content nor categories while normal imports remain green.
- 2026-07-10: Resolved the `chunk-22-export-import-b` P2 direct import fallback finding. The admin-post confirmation handler now shares the AJAX active-job gate, creates the durable import job, and redirects to the Import page where existing bootstrap code resumes processing; it no longer calls the full-zip importer. Updated the handler-flow regression to prove no content is created synchronously and that bounded job completion preserves preview cleanup, history, and undo.
- 2026-07-10: Resolved the `chunk-26-offline-app-and-builder` P2 uncapped Android PCM/STT finding. Offline speaking now rejects blobs over 8 MB and decoded audio over 15 seconds before base64 encoding; Android validates sample rate, channels, encoded size, decoded PCM bytes, and resampled samples before inference, with matching session and JNI sample ceilings. Added browser normal/oversized bridge coverage and JVM byte/sample budget tests; Java and all three native ABIs compile successfully.
- 2026-07-10: Resolved the `chunk-02-data-model-roles` P2 public vocab-lesson prompt-card detection finding. Initial lesson rendering now uses a shared category context and an ID-only prompt-card summary capped at three, with metadata and term cache priming disabled; full card expansion remains in the deferred grid request. Added a seven-card shell regression that verifies bounded query shape and three loading cards.
- 2026-07-10: Resolved the `chunk-18-dictionary-i18n-cache` P3 public linked-word count N+1 finding. Dictionary result pages now aggregate linked-word counts for visible entry IDs in one bounded SQL query and pass those counts into entry hydration, while direct single-entry callers retain the existing helper. Added a resource regression proving three visible cards use one aggregate count query and preserve zero/nonzero counts.
- 2026-07-10: Resolved the `chunk-14-audio-recorder-a` P2 new-word type-preflight scope finding. The preflight now sends current numeric and legacy wordset context, keys its cache by wordset and category, and the PHP handler rejects requests with no accessible explicit wordset instead of falling back to the default. Added focused browser request-contract and PHP strict-scope coverage.
- 2026-07-10: Resolved the `chunk-19-automation-rest-a` P2 missing-meta/bulk-update finding. Both routes now use bounded SQL ID scans before row hydration; sparse reads expose candidate offsets and writes persist a failure-safe `scan_after_id` cursor. Added sparse paging, resumable write, and legacy cap compatibility coverage.
- 2026-07-10: Resolved the `chunk-20-automation-rest-b-site-sync-cli` P2 REST report finding. `/report` now pages word IDs before building page-scoped coverage, exposes continuation metadata, and leaves full-wordset report generation to WP-CLI. Added a resource regression proving REST report pages do not run an unbounded words query.
- 2026-07-10: Resolved the `chunk-19-automation-rest-a` P2 REST category-scope finding. Category update and term-maintenance routes now use a shared aggregate category-ID helper that combines explicit ownership, scoped word relationships, and wordset-owned image relationships without hydrating all posts. Added legacy unowned-category resource coverage and reused the helper in Bulk Word Import.
- 2026-07-10: Resolved the `chunk-20-automation-rest-b-site-sync-cli` P2 transcription-validation candidate finding. Validation refreshes and job creation now select capped `word_audio` candidates through parent-word wordset joins and retain per-recording membership/staleness checks. Added focused cap, wordset-scope, and candidate-scope coverage.
- 2026-07-07: Resolved the `chunk-13-word-grid-b` P1 transcription remote-audio download finding. `ll_tools_resolve_audio_file_for_transcription()` now keeps same-site upload URLs local, rejects arbitrary remote URLs by default, requires explicit opt-in for remote transcription audio downloads, and reuses the private/reserved host and resolved-IP guards before `download_url()` can run. Added focused `SecurityHardeningRegressionTest` coverage for link-local URLs, public hostnames resolving to private IPs, and same-site upload URL resolution.
- 2026-07-07: Resolved the `chunk-15-audio-recorder-b-media-shortcodes` P2 bulk image upload scope finding. `[image_upload_form]` and `process_image_files` now require upload access, LL Tools access, and a real upload-management scope; non-global submissions are filtered to explicitly managed wordsets, empty non-global scope is rejected, and selected categories are validated against the chosen wordset scope. Added focused coverage for audio-recorder denial, manager-scoped rendering, and category-scope validation.
- 2026-07-07: Resolved the `chunk-20-automation-rest-b-site-sync-cli` P2 transcription and speaker update batch-cap finding. The transcription and word-audio speaker update routes now share exact-ID request normalization, reject over-limit arrays before mutation, expose cap metadata in `resource_guard`, and document the synchronous limits. Added focused `AutomationRestApiTest` coverage proving rejected over-cap payloads leave recording text, speaker metadata, and post authors unchanged.
- 2026-07-07: Resolved the `chunk-09-wordset-games-php-js-a` P2 speaking score/match guard finding. Speaking match scoring now rejects over-cap active candidate lists before scoring, reference-STT score and match AJAX paths share the existing user/IP speaking transcription rate-limit buckets before any cold reference STT can run, and focused `WordsetGamesTest` coverage proves rate-limited score requests and oversized match requests do not attempt external HTTP.
- 2026-07-08: Resolved the `chunk-27-docs-css-scripts` P2 stable release asset replacement finding. Stable publish now uploads a uniquely named temporary release asset, verifies its size, deletes the previous final-named zip only after the replacement is present, renames the verified replacement to the final asset name, and verifies the renamed asset. Added static `ReleasePluginScriptTest` coverage for the publish ordering and documented the safer replacement behavior in `RELEASING.md`.
- 2026-07-08: Resolved the `chunk-11-vocab-content-lessons` P2 corpus text grid default boundedness finding. `[ll_corpus_text_grid]` now defaults to a filterable bounded page size, keeps `limit="-1"` / `limit="all"` as explicit unbounded opt-ins, supports paged corpus grid requests, and renders pagination links when a collection has more texts than the current page. Added focused `ContentLessonCueParsingTest` coverage proving the default shortcode renders only the bounded page while page 2 and explicit unbounded requests still retrieve the expected texts.
- 2026-07-08: Resolved the `chunk-05-wordset-pages-1` P2 inactive-category preview GET mutation finding. Inactive preview card fallback links now point to the safe wordset page URL instead of a nonce-bearing action query, while the hidden POST form remains the JS-driven preview mutation path. The template redirect action handler now ignores GET inactive-category actions, so legacy preview URLs are read-only. Added focused `WordsetPageInactiveCategoryCardsTest` coverage proving rendered preview links do not expose action query strings and direct GET preview requests do not create image-backed draft words.
- 2026-07-10: Resolved the `chunk-03-wordset-category-taxonomies` P2 grammar-option rewrite finding. Gender, plurality, verb-tense, and verb-mood label changes now append ordered durable tasks instead of scanning words during the term-save request. A locked ID-cursor worker processes 100 words by default with a hard 250-row cap, aggregates category cache invalidation per batch, preserves tasks appended during an active batch, and exposes translated queued/running/completed/failed readback on Wordset admin screens. Added 2/2/1 batch, consecutive-remap ordering, and in-flight append regressions.
- 2026-07-10: Resolved the `chunk-23-export-import-c-admin-imports` P2 synchronous STT training export finding. Starting an STT export now creates only a user-scoped durable manifest. Locked continuation requests keyset-page recordings in word-title/word-ID/recording-ID order, with a 25-recording default and 100-recording hard cap, append only that batch's audio and streamed CSV/JSONL rows, and publish the download only after final metadata closeout. The non-JavaScript fallback creates the same job and exposes bounded Continue steps with progress. Added a five-recording 2/2/1 resource regression and a browser endpoint/progress/download contract.
- 2026-07-10: Resolved the `chunk-16-flashcard-quiz-runtime-a` P2 synchronous quiz-page maintenance finding. Daily, manual, source-change, and forced cleanup paths now use one locked durable job with cleanup and category-sync phases, ID cursors, a 25-row default, and a 100-row hard cap. Routine cleanup preserves missing-category shells; only explicit forced cleanup permanently deletes orphans. Admin surfaces show queued progress, and completion records the daily timestamp and clears stale continuations. Added bounded multi-category cursor/query-shape coverage and forced-orphan policy coverage; existing post-type, deferred-maintenance, and manager-import suites remain green.
- 2026-07-10: Resolved the `chunk-24-ipa-keyboard-a` P2 implicit Genç-Palu profile finding. A one-time set-based migration explicitly preserves `zazaki_genc_palu` on existing `zza`/`diq`/`kiu`/Zazaki/Zaza wordsets and shows administrators a translated seven-day review notice. Profile resolution now reads only valid explicit term meta, so new generic Zazaki wordsets have no regional rules. The Wordset taxonomy form exposes a translated None/Genç-Palu selector with validated save/clear behavior. Added generic-versus-explicit, one-time migration, deep profile, and admin UI regressions.

## Completed Chunks

- `chunk-28-tests-languages-i18n` completed 2026-07-07 15:20 Europe/Istanbul. Reviewed PHPUnit/Playwright test infrastructure, Local bootstrap scripts, live-smoke guards, performance fixture runner docs, tracked test helpers, public i18n manifest/source policy/tests, locale catalogs, and tier-2 DeepL/check/update scripts. No new findings were recorded; `php scripts/check-public-i18n.php --all-tier2 --json` confirmed the manifest matches the current POT selection and all active tier-2 locales cover the 646 public strings with compiled assets, while planned locales remain intentionally missing.

- `chunk-27-docs-css-scripts` completed 2026-07-07 15:09 Europe/Istanbul. Reviewed release packaging docs and scripts, AI context docs and generator contracts, maintenance source-contract Playwright spec, runtime asset manifest, top-level docs, CSS surfaces, git attributes/ignore rules, and command wrappers. Recorded one finding above for stable release asset replacement ordering; no source-code fix was made because the release workflow needs a deliberate mocked GitHub API test harness or transactional upload contract.

- `chunk-26-offline-app-and-builder` completed 2026-07-07 14:59 Europe/Istanbul. Reviewed offline app sync/auth/throttle handlers, offline export admin/bundle construction helpers, offline shell bootstrap and sync/STT bridge code, offline app builder scripts, Android/ JNI STT overrides, and focused offline export/sync/browser coverage. Recorded three findings above for synchronous full-bundle export, eager export-page category preload, and uncapped Android PCM/STT payload handling; no safe source-code fix was made because the fixes need a bounded export job contract, lazy category endpoint behavior, or Android bridge guard/test coverage.

- `chunk-25-ipa-keyboard-b-admin-media` completed 2026-07-07 14:52 Europe/Istanbul. Reviewed intervening locale-refresh commit `747bef95a67f788a596ae6b8f191bb365e9fe26a`, reconciled the heartbeat allowed HEAD per user instruction, then reviewed `includes/admin/ipa-keyboard-admin.php` lines 4501-8656, `js/ipa-keyboard-admin.js` lines 2701-5303, admin audio/image/media maintenance PHP, audio/image admin JS and CSS, `templates/audio-image-matcher-template.php`, and focused IPA/audio/image integration coverage. Recorded four findings above for the Audio/Image Matcher image picker, Missing Audio maintenance actions, Image Aspect Normalizer worklist/row-action scans, and the legacy Word Images fixer; no safe source-code fix was made because the fixes need bounded job or paged endpoint contracts.

- `chunk-01-core-runtime` completed 2026-07-07 00:28 Europe/Istanbul. Reviewed entry point/update hooks, bootstrap load order, asset enqueue/cache guards, template loader, flashcard shell, PHP compatibility helper, sort/text helpers, and plugin templates. No findings recorded for this chunk.
- `chunk-02-data-model-roles` completed 2026-07-07 00:28 Europe/Istanbul. Reviewed post types, smaller taxonomies, role helpers, privacy/login/class runtime, and related admin/public hook surfaces. Recorded three boundedness findings above; no safe source-code fix was made in this heartbeat because the fixes need lazy-loading or query-shape changes with focused tests.
- `chunk-03-wordset-category-taxonomies` completed 2026-07-07 00:34 Europe/Istanbul. Reviewed word-category and wordset taxonomy helpers, wordset isolation/health code, wordset template cloning helpers, wordset language settings, entity translations, word translation helpers, and relevant integration-test coverage. Recorded three boundedness findings above; no safe source-code fix was made in this heartbeat because the fixes need paged UI, durable jobs, or async maintenance flows with focused tests.
- `chunk-04-user-progress-study` completed 2026-07-07 00:50 Europe/Istanbul. Reviewed user progress persistence, study state, learner progress reporting, recommendation/analytics helpers, related AJAX handlers, and matching integration-test coverage. Recorded two boundedness findings above; no safe source-code fix was made in this heartbeat because the fixes need report query redesign and bounded recommendation refresh behavior with focused tests.
- `chunk-05-wordset-pages-1` completed 2026-07-07 00:50 Europe/Istanbul. Reviewed `includes/pages/wordset-pages.php` lines 1-4650, including early route helpers, cache/query helpers, category summaries, inactive-category actions, preview construction, and first-pass category row builders. Recorded one GET-mutation/resource finding above; no safe source-code fix was made in this heartbeat because the fix needs a POST/batch workflow and regression coverage.
- `chunk-06-wordset-pages-2` completed 2026-07-07 01:12 Europe/Istanbul. Reviewed `includes/pages/wordset-pages.php` lines 4651-9300, including lazy-card/category-search cache helpers, mixed-card rendering, wordset route URL helpers, manager settings/import/template actions, offline export action, and recorder/manager invite token helpers. No unresolved findings were recorded for this chunk. Fixed a safe inactive-card markup regression where preview-only inactive cards opened an anchor but closed a `div`, and added focused integration assertions for balanced inactive-card links.
- `chunk-07-wordset-pages-3` completed 2026-07-07 01:32 Europe/Istanbul. Reviewed `includes/pages/wordset-pages.php` lines 9301-13950, including manager/recorder invite acceptance, recorder access/queue POST and AJAX handlers, progress reset handling, teacher classes rendering, games config/i18n, front-end utility menu, settings hub/tool rendering, advanced settings payloads, category management helpers, and transcription settings UI. Recorded two boundedness findings above; no safe source-code fix was made in this heartbeat because the fixes need paged/searchable manager surfaces, aggregate counts, and batch/delete workflows with focused resource tests.
- `chunk-08-wordset-pages-4` completed 2026-07-07 01:51 Europe/Istanbul. Reviewed `includes/pages/wordset-pages.php` lines 13951-18591, `templates/wordset-page-template.php`, `css/wordset-pages.css`, focused `WordsetPage*` integration tests, and focused wordset-page/manager Playwright specs. Recorded three boundedness/resource findings above; no safe source-code fix was made in this heartbeat because the fixes need view-specific settings prework, cached/paged recorder queue summaries, and content-lesson render gating with resource tests.
- `chunk-09-wordset-games-php-js-a` completed 2026-07-07 02:12 Europe/Istanbul. Reviewed `includes/pages/wordset-games.php`, `css/wordset-games.css`, and `js/wordset-games.js` lines 1-5500, including game catalog rendering, practice/Unscramble/Line Up/speaking pool builders, launch and speaking AJAX handlers, early canvas/preload/runtime helpers, localized game-card markup, and focused Wordset Games test coverage. Recorded three resource/guard findings above; no safe source-code fix was made in this heartbeat because the fixes need bounded launch payload contracts, endpoint caps/rate guards, and focused resource tests.
- `chunk-10-wordset-games-js-b` completed 2026-07-07 02:31 Europe/Istanbul. Reviewed `js/wordset-games.js` lines 5501-12359, including the actual EOF because the manifest range ends at the stale line 11092, plus `tests/Integration/WordsetGamesTest.php` and `tests/e2e/specs/wordset-games-space-shooter.spec.js`. No new findings were recorded for this chunk; the notable speaking score/match guard gap remains tracked under the `chunk-09-wordset-games-php-js-a` findings above. No source-code fix was made in this heartbeat.
- `chunk-11-vocab-content-lessons` completed 2026-07-07 02:51 Europe/Istanbul. Reviewed vocab/content lesson page helpers, CPT/admin metadata handlers, lesson templates, lesson JS/CSS, focused integration tests, and focused Playwright specs. Recorded three boundedness/resource findings above; no safe source-code fix was made in this heartbeat because the fixes need bounded query contracts, pagination/preparation UX, and focused resource tests.
- `chunk-12-wordset-editor-word-grid-a` completed 2026-07-07 03:11 Europe/Istanbul. Reviewed `includes/pages/wordset-editor.php`, `css/editor-hub.css`, `js/editor-hub.js`, `includes/shortcodes/word-grid-shortcode.php` lines 1-5000 with adjacent shell-card call context, and `js/word-grid.js` lines 1-4500. Recorded three boundedness/resource findings above; no safe source-code fix was made in this heartbeat because the fixes need paged/indexed query contracts, bounded bulk-job behavior, or deferred shell contract changes with focused resource tests.
- `chunk-13-word-grid-b` completed 2026-07-07 03:47 Europe/Istanbul. Reviewed `includes/shortcodes/word-grid-shortcode.php` lines 5001-10619 because the manifest's 9560 end line is stale, `js/word-grid.js` lines 4501-10061 because the manifest's 9039 end line is stale, `js/word-edit-modal.js`, `js/bulk-category-edit.js`, `js/vocab-lesson-word-options-modal.js`, `css/vocab-lesson-word-options-modal.css`, focused `WordGrid*` integration tests, and `WordsetEditorToolTest`. Recorded three resource/security findings above; no safe source-code fix was made in this heartbeat because the fixes need bounded job/query contracts and URL validation coverage.
- `chunk-14-audio-recorder-a` completed 2026-07-07 03:51 Europe/Istanbul. Reviewed `includes/shortcodes/audio-recording-shortcode.php` lines 1-3500 with adjacent queue helper and request-context callsites, `js/audio-recorder.js` lines 1-2400 with adjacent new-word type preflight callsite, `css/recording-interface.css`, `includes/pages/recording-page.php`, focused recorder helper tests, and existing maintenance docs. Recorded two findings above; no safe source-code fix was made in this heartbeat because the fixes need a bounded recorder bootstrap contract and multi-wordset request-context coverage.
- `chunk-15-audio-recorder-b-media-shortcodes` completed 2026-07-07 04:22 Europe/Istanbul. Reviewed `includes/shortcodes/audio-recording-shortcode.php` lines 3501-7577 because the manifest's 6706 end line is stale, `js/audio-recorder.js` lines 2401-5294 because the manifest's 4670 end line is stale, `includes/shortcodes/word-audio-shortcode.php`, `includes/shortcodes/audio-credit-grid-shortcode.php`, upload helper PHP files, `js/word-audio*.js`, and focused audio recorder integration/E2E tests. Recorded one unresolved image-upload capability finding above. Fixed a scoped recorder guard so submitted `word_id` requests must belong to the requested recorder wordset before the upload/verify/skip flows can treat them as accessible uncategorized queue items; added focused helper coverage.
- `chunk-16-flashcard-quiz-runtime-a` completed 2026-07-07 04:32 Europe/Istanbul. Reviewed flashcard widget/quiz-page shortcode PHP, quiz page routing/sync/embed helpers, flashcard and quiz-page templates, flashcard base/mode CSS, quiz-page CSS, `js/quiz-pages*.js`, and the core flashcard `main.js`, `selection.js`, and `loader.js` runtime. Recorded three boundedness findings above; no safe source-code fix was made in this heartbeat because the fixes need bounded catalog/index/job contracts and focused resource tests rather than a narrow local patch.
- `chunk-17-flashcard-quiz-runtime-b` completed 2026-07-07 04:51 Europe/Istanbul. Reviewed flashcard mode/component JS, option-conflict helpers, Word Option Rules PHP/admin JS/CSS, and focused flashcard/option/listening/quiz browser and integration coverage. Recorded two boundedness findings above; no safe source-code fix was made in this heartbeat because the fixes need bounded editor and listening-prefetch contracts with focused resource tests.
- `chunk-18-dictionary-i18n-cache` completed 2026-07-07 05:14 Europe/Istanbul. Reviewed dictionary browser/search-index/snapshot/static-cache helpers, public static cache, dictionary and language-switcher shortcodes/assets, focused dictionary/static-cache integration tests, and the dictionary deferred-toolbar browser spec. Recorded three dictionary boundedness/query findings above; no safe source-code fix was made in this heartbeat because the fixes need deferred toolbar activation, cursor-backed snapshot/import phases, and batched linked-count contracts with focused resource tests.
- `chunk-19-automation-rest-a` completed 2026-07-07 05:31 Europe/Istanbul. Reviewed `includes/api/automation-rest.php` lines 1-5000, the shared CLI/editor helpers used by those REST paths, `AutomationRestApiTest`, `AutomationRestResourceGuardTest`, and REST automation docs. Recorded two boundedness findings above; no safe source-code fix was made in this heartbeat because the fixes need cursor-backed query contracts or a lightweight REST category-scope resolver with focused resource tests.
- `chunk-20-automation-rest-b-site-sync-cli` completed 2026-07-07 05:52 Europe/Istanbul. Reviewed `includes/api/automation-rest.php` lines 5001-10577 because the manifest's 9465 end line is stale, `includes/lib/site-sync.php`, `includes/admin/site-sync-admin.php`, `includes/cli/*.php`, Site Sync admin JS/CSS, local bin helpers, and `SiteSyncTest`. Recorded three REST boundedness findings above; no safe source-code fix was made in this heartbeat because the fixes need explicit REST caps, bounded candidate queries, or pagination/job contracts with focused resource tests.
- `chunk-21-export-import-a` completed 2026-07-07 06:11 Europe/Istanbul. Reviewed `includes/admin/export-import.php` lines 1-4500, `js/export-import-admin.js`, and `css/export-import-admin.css`, including export/import admin registration, export batching shell, import preview media streaming, import job bookkeeping, selector rendering, recent import summaries, and admin UI scripts. Recorded two export/import boundedness findings above. Fixed a malformed CSS fragment in `css/export-import-admin.css` that left an orphan declaration and extra closing brace after the mobile import-preview media rules.
- `chunk-22-export-import-b` completed 2026-07-07 06:31 Europe/Istanbul. Reviewed `includes/admin/export-import.php` lines 4501-9000, focused import/export integration tests, adjacent import-job/direct-import callsites, and the normal import JS submit interception. Recorded three boundedness/resource findings above; no safe source-code fix was made in this heartbeat because the fixes need preview/job/cursor contracts and focused resource tests rather than a narrow local patch.
- `chunk-23-export-import-c-admin-imports` completed 2026-07-07 06:51 Europe/Istanbul. Reviewed `includes/admin/export-import.php` lines 9001-13250, import/export job tail and STT export helpers, bulk word/translation import pages, dictionary import and source admin pages, prompt-audio import, settings/admin dashboard menu, dictionary import JS/CSS, bulk-word import JS, and the admin import preview/undo browser spec. Recorded four boundedness/resource findings above; no safe source-code fix was made in this heartbeat because the fixes need job/cursor contracts or deliberate row-cap behavior with focused resource tests.
- `chunk-24-ipa-keyboard-a` completed 2026-07-07 07:08 Europe/Istanbul. Reviewed `includes/admin/ipa-keyboard-admin.php` lines 1-4500, adjacent AJAX callsites needed to verify first-range helpers, `js/ipa-keyboard-admin.js` lines 1-2700, `css/ipa-keyboard-admin.css`, `css/ipa-fonts.css`, and focused IPA keyboard/search/orthography integration and browser coverage. Recorded three findings above covering whole-wordset symbol/map hydration, search pagination after full scans, and implicit Genç-Palu profile selection for all Zazaki wordsets; no safe source-code fix was made in this heartbeat because the fixes need bounded endpoint contracts or an explicit profile migration decision.
